/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { PaycodeWebhookLogService } from "../paycodeWebhookLog.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { PaycodeWebhookLogCreateInput } from "./PaycodeWebhookLogCreateInput";
import { PaycodeWebhookLog } from "./PaycodeWebhookLog";
import { PaycodeWebhookLogFindManyArgs } from "./PaycodeWebhookLogFindManyArgs";
import { PaycodeWebhookLogWhereUniqueInput } from "./PaycodeWebhookLogWhereUniqueInput";
import { PaycodeWebhookLogUpdateInput } from "./PaycodeWebhookLogUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class PaycodeWebhookLogControllerBase {
  constructor(
    protected readonly service: PaycodeWebhookLogService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: PaycodeWebhookLog })
  @nestAccessControl.UseRoles({
    resource: "PaycodeWebhookLog",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createPaycodeWebhookLog(
    @common.Body() data: PaycodeWebhookLogCreateInput
  ): Promise<PaycodeWebhookLog> {
    return await this.service.createPaycodeWebhookLog({
      data: {
        ...data,

        paycodePaymentTransacs: data.paycodePaymentTransacs
          ? {
              connect: data.paycodePaymentTransacs,
            }
          : undefined,
      },
      select: {
        fechaRegistro: true,
        id: true,

        paycodePaymentTransacs: {
          select: {
            id: true,
          },
        },

        response: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [PaycodeWebhookLog] })
  @ApiNestedQuery(PaycodeWebhookLogFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "PaycodeWebhookLog",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async paycodeWebhookLogs(
    @common.Req() request: Request
  ): Promise<PaycodeWebhookLog[]> {
    const args = plainToClass(PaycodeWebhookLogFindManyArgs, request.query);
    return this.service.paycodeWebhookLogs({
      ...args,
      select: {
        fechaRegistro: true,
        id: true,

        paycodePaymentTransacs: {
          select: {
            id: true,
          },
        },

        response: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: PaycodeWebhookLog })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "PaycodeWebhookLog",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async paycodeWebhookLog(
    @common.Param() params: PaycodeWebhookLogWhereUniqueInput
  ): Promise<PaycodeWebhookLog | null> {
    const result = await this.service.paycodeWebhookLog({
      where: params,
      select: {
        fechaRegistro: true,
        id: true,

        paycodePaymentTransacs: {
          select: {
            id: true,
          },
        },

        response: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: PaycodeWebhookLog })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "PaycodeWebhookLog",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updatePaycodeWebhookLog(
    @common.Param() params: PaycodeWebhookLogWhereUniqueInput,
    @common.Body() data: PaycodeWebhookLogUpdateInput
  ): Promise<PaycodeWebhookLog | null> {
    try {
      return await this.service.updatePaycodeWebhookLog({
        where: params,
        data: {
          ...data,

          paycodePaymentTransacs: data.paycodePaymentTransacs
            ? {
                connect: data.paycodePaymentTransacs,
              }
            : undefined,
        },
        select: {
          fechaRegistro: true,
          id: true,

          paycodePaymentTransacs: {
            select: {
              id: true,
            },
          },

          response: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: PaycodeWebhookLog })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "PaycodeWebhookLog",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deletePaycodeWebhookLog(
    @common.Param() params: PaycodeWebhookLogWhereUniqueInput
  ): Promise<PaycodeWebhookLog | null> {
    try {
      return await this.service.deletePaycodeWebhookLog({
        where: params,
        select: {
          fechaRegistro: true,
          id: true,

          paycodePaymentTransacs: {
            select: {
              id: true,
            },
          },

          response: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
