/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { StockSeryService } from "../stockSery.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { StockSeryCreateInput } from "./StockSeryCreateInput";
import { StockSery } from "./StockSery";
import { StockSeryFindManyArgs } from "./StockSeryFindManyArgs";
import { StockSeryWhereUniqueInput } from "./StockSeryWhereUniqueInput";
import { StockSeryUpdateInput } from "./StockSeryUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class StockSeryControllerBase {
  constructor(
    protected readonly service: StockSeryService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: StockSery })
  @nestAccessControl.UseRoles({
    resource: "StockSery",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createStockSery(
    @common.Body() data: StockSeryCreateInput
  ): Promise<StockSery> {
    return await this.service.createStockSery({
      data: {
        ...data,

        items: {
          connect: data.items,
        },

        warehouses: {
          connect: data.warehouses,
        },
      },
      select: {
        codigoBloqueo: true,
        estatus_bloqueo: true,
        estatus_serie: true,
        id: true,

        items: {
          select: {
            id: true,
          },
        },

        otraSerie: true,
        proveedorBloqueo: true,
        serialCode: true,
        serieEnrolada: true,

        warehouses: {
          select: {
            id: true,
          },
        },
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [StockSery] })
  @ApiNestedQuery(StockSeryFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "StockSery",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async stockSeries(@common.Req() request: Request): Promise<StockSery[]> {
    const args = plainToClass(StockSeryFindManyArgs, request.query);
    return this.service.stockSeries({
      ...args,
      select: {
        codigoBloqueo: true,
        estatus_bloqueo: true,
        estatus_serie: true,
        id: true,

        items: {
          select: {
            id: true,
          },
        },

        otraSerie: true,
        proveedorBloqueo: true,
        serialCode: true,
        serieEnrolada: true,

        warehouses: {
          select: {
            id: true,
          },
        },
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: StockSery })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "StockSery",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async stockSery(
    @common.Param() params: StockSeryWhereUniqueInput
  ): Promise<StockSery | null> {
    const result = await this.service.stockSery({
      where: params,
      select: {
        codigoBloqueo: true,
        estatus_bloqueo: true,
        estatus_serie: true,
        id: true,

        items: {
          select: {
            id: true,
          },
        },

        otraSerie: true,
        proveedorBloqueo: true,
        serialCode: true,
        serieEnrolada: true,

        warehouses: {
          select: {
            id: true,
          },
        },
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: StockSery })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "StockSery",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateStockSery(
    @common.Param() params: StockSeryWhereUniqueInput,
    @common.Body() data: StockSeryUpdateInput
  ): Promise<StockSery | null> {
    try {
      return await this.service.updateStockSery({
        where: params,
        data: {
          ...data,

          items: {
            connect: data.items,
          },

          warehouses: {
            connect: data.warehouses,
          },
        },
        select: {
          codigoBloqueo: true,
          estatus_bloqueo: true,
          estatus_serie: true,
          id: true,

          items: {
            select: {
              id: true,
            },
          },

          otraSerie: true,
          proveedorBloqueo: true,
          serialCode: true,
          serieEnrolada: true,

          warehouses: {
            select: {
              id: true,
            },
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: StockSery })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "StockSery",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteStockSery(
    @common.Param() params: StockSeryWhereUniqueInput
  ): Promise<StockSery | null> {
    try {
      return await this.service.deleteStockSery({
        where: params,
        select: {
          codigoBloqueo: true,
          estatus_bloqueo: true,
          estatus_serie: true,
          id: true,

          items: {
            select: {
              id: true,
            },
          },

          otraSerie: true,
          proveedorBloqueo: true,
          serialCode: true,
          serieEnrolada: true,

          warehouses: {
            select: {
              id: true,
            },
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
