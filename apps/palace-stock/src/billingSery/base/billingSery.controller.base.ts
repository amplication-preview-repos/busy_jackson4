/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { BillingSeryService } from "../billingSery.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { BillingSeryCreateInput } from "./BillingSeryCreateInput";
import { BillingSery } from "./BillingSery";
import { BillingSeryFindManyArgs } from "./BillingSeryFindManyArgs";
import { BillingSeryWhereUniqueInput } from "./BillingSeryWhereUniqueInput";
import { BillingSeryUpdateInput } from "./BillingSeryUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class BillingSeryControllerBase {
  constructor(
    protected readonly service: BillingSeryService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: BillingSery })
  @nestAccessControl.UseRoles({
    resource: "BillingSery",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createBillingSery(
    @common.Body() data: BillingSeryCreateInput
  ): Promise<BillingSery> {
    return await this.service.createBillingSery({
      data: data,
      select: {
        descripcion: true,
        id: true,
        serieFactura: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [BillingSery] })
  @ApiNestedQuery(BillingSeryFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "BillingSery",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async billingSeries(@common.Req() request: Request): Promise<BillingSery[]> {
    const args = plainToClass(BillingSeryFindManyArgs, request.query);
    return this.service.billingSeries({
      ...args,
      select: {
        descripcion: true,
        id: true,
        serieFactura: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: BillingSery })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "BillingSery",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async billingSery(
    @common.Param() params: BillingSeryWhereUniqueInput
  ): Promise<BillingSery | null> {
    const result = await this.service.billingSery({
      where: params,
      select: {
        descripcion: true,
        id: true,
        serieFactura: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: BillingSery })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "BillingSery",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateBillingSery(
    @common.Param() params: BillingSeryWhereUniqueInput,
    @common.Body() data: BillingSeryUpdateInput
  ): Promise<BillingSery | null> {
    try {
      return await this.service.updateBillingSery({
        where: params,
        data: data,
        select: {
          descripcion: true,
          id: true,
          serieFactura: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: BillingSery })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "BillingSery",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteBillingSery(
    @common.Param() params: BillingSeryWhereUniqueInput
  ): Promise<BillingSery | null> {
    try {
      return await this.service.deleteBillingSery({
        where: params,
        select: {
          descripcion: true,
          id: true,
          serieFactura: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
