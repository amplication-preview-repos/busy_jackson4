/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { BillingCfdiUsService } from "../billingCfdiUs.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { BillingCfdiUsCreateInput } from "./BillingCfdiUsCreateInput";
import { BillingCfdiUs } from "./BillingCfdiUs";
import { BillingCfdiUsFindManyArgs } from "./BillingCfdiUsFindManyArgs";
import { BillingCfdiUsWhereUniqueInput } from "./BillingCfdiUsWhereUniqueInput";
import { BillingCfdiUsUpdateInput } from "./BillingCfdiUsUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class BillingCfdiUsControllerBase {
  constructor(
    protected readonly service: BillingCfdiUsService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: BillingCfdiUs })
  @nestAccessControl.UseRoles({
    resource: "BillingCfdiUs",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createBillingCfdiUs(
    @common.Body() data: BillingCfdiUsCreateInput
  ): Promise<BillingCfdiUs> {
    return await this.service.createBillingCfdiUs({
      data: data,
      select: {
        aplicaPersFisica: true,
        aplicaPersMoral: true,
        clave: true,
        descripcion: true,
        id: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [BillingCfdiUs] })
  @ApiNestedQuery(BillingCfdiUsFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "BillingCfdiUs",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async billingCfdiuses(
    @common.Req() request: Request
  ): Promise<BillingCfdiUs[]> {
    const args = plainToClass(BillingCfdiUsFindManyArgs, request.query);
    return this.service.billingCfdiuses({
      ...args,
      select: {
        aplicaPersFisica: true,
        aplicaPersMoral: true,
        clave: true,
        descripcion: true,
        id: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: BillingCfdiUs })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "BillingCfdiUs",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async billingCfdiUs(
    @common.Param() params: BillingCfdiUsWhereUniqueInput
  ): Promise<BillingCfdiUs | null> {
    const result = await this.service.billingCfdiUs({
      where: params,
      select: {
        aplicaPersFisica: true,
        aplicaPersMoral: true,
        clave: true,
        descripcion: true,
        id: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: BillingCfdiUs })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "BillingCfdiUs",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateBillingCfdiUs(
    @common.Param() params: BillingCfdiUsWhereUniqueInput,
    @common.Body() data: BillingCfdiUsUpdateInput
  ): Promise<BillingCfdiUs | null> {
    try {
      return await this.service.updateBillingCfdiUs({
        where: params,
        data: data,
        select: {
          aplicaPersFisica: true,
          aplicaPersMoral: true,
          clave: true,
          descripcion: true,
          id: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: BillingCfdiUs })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "BillingCfdiUs",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteBillingCfdiUs(
    @common.Param() params: BillingCfdiUsWhereUniqueInput
  ): Promise<BillingCfdiUs | null> {
    try {
      return await this.service.deleteBillingCfdiUs({
        where: params,
        select: {
          aplicaPersFisica: true,
          aplicaPersMoral: true,
          clave: true,
          descripcion: true,
          id: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
