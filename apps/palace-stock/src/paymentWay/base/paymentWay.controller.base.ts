/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { PaymentWayService } from "../paymentWay.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { PaymentWayCreateInput } from "./PaymentWayCreateInput";
import { PaymentWay } from "./PaymentWay";
import { PaymentWayFindManyArgs } from "./PaymentWayFindManyArgs";
import { PaymentWayWhereUniqueInput } from "./PaymentWayWhereUniqueInput";
import { PaymentWayUpdateInput } from "./PaymentWayUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class PaymentWayControllerBase {
  constructor(
    protected readonly service: PaymentWayService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: PaymentWay })
  @nestAccessControl.UseRoles({
    resource: "PaymentWay",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createPaymentWay(
    @common.Body() data: PaymentWayCreateInput
  ): Promise<PaymentWay> {
    return await this.service.createPaymentWay({
      data: data,
      select: {
        descripcion: true,
        id: true,
        numFormaPago: true,
        paymentWay: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [PaymentWay] })
  @ApiNestedQuery(PaymentWayFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "PaymentWay",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async paymentWays(@common.Req() request: Request): Promise<PaymentWay[]> {
    const args = plainToClass(PaymentWayFindManyArgs, request.query);
    return this.service.paymentWays({
      ...args,
      select: {
        descripcion: true,
        id: true,
        numFormaPago: true,
        paymentWay: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: PaymentWay })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "PaymentWay",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async paymentWay(
    @common.Param() params: PaymentWayWhereUniqueInput
  ): Promise<PaymentWay | null> {
    const result = await this.service.paymentWay({
      where: params,
      select: {
        descripcion: true,
        id: true,
        numFormaPago: true,
        paymentWay: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: PaymentWay })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "PaymentWay",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updatePaymentWay(
    @common.Param() params: PaymentWayWhereUniqueInput,
    @common.Body() data: PaymentWayUpdateInput
  ): Promise<PaymentWay | null> {
    try {
      return await this.service.updatePaymentWay({
        where: params,
        data: data,
        select: {
          descripcion: true,
          id: true,
          numFormaPago: true,
          paymentWay: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: PaymentWay })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "PaymentWay",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deletePaymentWay(
    @common.Param() params: PaymentWayWhereUniqueInput
  ): Promise<PaymentWay | null> {
    try {
      return await this.service.deletePaymentWay({
        where: params,
        select: {
          descripcion: true,
          id: true,
          numFormaPago: true,
          paymentWay: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
