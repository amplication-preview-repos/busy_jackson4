/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { PaycodeApiLogService } from "../paycodeApiLog.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { PaycodeApiLogCreateInput } from "./PaycodeApiLogCreateInput";
import { PaycodeApiLog } from "./PaycodeApiLog";
import { PaycodeApiLogFindManyArgs } from "./PaycodeApiLogFindManyArgs";
import { PaycodeApiLogWhereUniqueInput } from "./PaycodeApiLogWhereUniqueInput";
import { PaycodeApiLogUpdateInput } from "./PaycodeApiLogUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class PaycodeApiLogControllerBase {
  constructor(
    protected readonly service: PaycodeApiLogService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: PaycodeApiLog })
  @nestAccessControl.UseRoles({
    resource: "PaycodeApiLog",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createPaycodeApiLog(
    @common.Body() data: PaycodeApiLogCreateInput
  ): Promise<PaycodeApiLog> {
    return await this.service.createPaycodeApiLog({
      data: {
        ...data,

        paycodePaymentTransacs: data.paycodePaymentTransacs
          ? {
              connect: data.paycodePaymentTransacs,
            }
          : undefined,
      },
      select: {
        callDate: true,
        calledMethod: true,
        callTime: true,
        calltrace: true,
        dataSend: true,
        headers: true,
        id: true,

        paycodePaymentTransacs: {
          select: {
            id: true,
          },
        },

        response: true,
        responseDatetime: true,
        url: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [PaycodeApiLog] })
  @ApiNestedQuery(PaycodeApiLogFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "PaycodeApiLog",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async paycodeApiLogs(
    @common.Req() request: Request
  ): Promise<PaycodeApiLog[]> {
    const args = plainToClass(PaycodeApiLogFindManyArgs, request.query);
    return this.service.paycodeApiLogs({
      ...args,
      select: {
        callDate: true,
        calledMethod: true,
        callTime: true,
        calltrace: true,
        dataSend: true,
        headers: true,
        id: true,

        paycodePaymentTransacs: {
          select: {
            id: true,
          },
        },

        response: true,
        responseDatetime: true,
        url: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: PaycodeApiLog })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "PaycodeApiLog",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async paycodeApiLog(
    @common.Param() params: PaycodeApiLogWhereUniqueInput
  ): Promise<PaycodeApiLog | null> {
    const result = await this.service.paycodeApiLog({
      where: params,
      select: {
        callDate: true,
        calledMethod: true,
        callTime: true,
        calltrace: true,
        dataSend: true,
        headers: true,
        id: true,

        paycodePaymentTransacs: {
          select: {
            id: true,
          },
        },

        response: true,
        responseDatetime: true,
        url: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: PaycodeApiLog })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "PaycodeApiLog",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updatePaycodeApiLog(
    @common.Param() params: PaycodeApiLogWhereUniqueInput,
    @common.Body() data: PaycodeApiLogUpdateInput
  ): Promise<PaycodeApiLog | null> {
    try {
      return await this.service.updatePaycodeApiLog({
        where: params,
        data: {
          ...data,

          paycodePaymentTransacs: data.paycodePaymentTransacs
            ? {
                connect: data.paycodePaymentTransacs,
              }
            : undefined,
        },
        select: {
          callDate: true,
          calledMethod: true,
          callTime: true,
          calltrace: true,
          dataSend: true,
          headers: true,
          id: true,

          paycodePaymentTransacs: {
            select: {
              id: true,
            },
          },

          response: true,
          responseDatetime: true,
          url: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: PaycodeApiLog })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "PaycodeApiLog",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deletePaycodeApiLog(
    @common.Param() params: PaycodeApiLogWhereUniqueInput
  ): Promise<PaycodeApiLog | null> {
    try {
      return await this.service.deletePaycodeApiLog({
        where: params,
        select: {
          callDate: true,
          calledMethod: true,
          callTime: true,
          calltrace: true,
          dataSend: true,
          headers: true,
          id: true,

          paycodePaymentTransacs: {
            select: {
              id: true,
            },
          },

          response: true,
          responseDatetime: true,
          url: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
