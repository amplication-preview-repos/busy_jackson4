/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { ArticleToTagService } from "../articleToTag.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { ArticleToTagCreateInput } from "./ArticleToTagCreateInput";
import { ArticleToTag } from "./ArticleToTag";
import { ArticleToTagFindManyArgs } from "./ArticleToTagFindManyArgs";
import { ArticleToTagWhereUniqueInput } from "./ArticleToTagWhereUniqueInput";
import { ArticleToTagUpdateInput } from "./ArticleToTagUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class ArticleToTagControllerBase {
  constructor(
    protected readonly service: ArticleToTagService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: ArticleToTag })
  @nestAccessControl.UseRoles({
    resource: "ArticleToTag",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createArticleToTag(
    @common.Body() data: ArticleToTagCreateInput
  ): Promise<ArticleToTag> {
    return await this.service.createArticleToTag({
      data: {
        ...data,

        article: {
          connect: data.article,
        },

        tag: {
          connect: data.tag,
        },
      },
      select: {
        article: {
          select: {
            id: true,
          },
        },

        id: true,

        tag: {
          select: {
            id: true,
          },
        },
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [ArticleToTag] })
  @ApiNestedQuery(ArticleToTagFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "ArticleToTag",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async articleToTags(@common.Req() request: Request): Promise<ArticleToTag[]> {
    const args = plainToClass(ArticleToTagFindManyArgs, request.query);
    return this.service.articleToTags({
      ...args,
      select: {
        article: {
          select: {
            id: true,
          },
        },

        id: true,

        tag: {
          select: {
            id: true,
          },
        },
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: ArticleToTag })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "ArticleToTag",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async articleToTag(
    @common.Param() params: ArticleToTagWhereUniqueInput
  ): Promise<ArticleToTag | null> {
    const result = await this.service.articleToTag({
      where: params,
      select: {
        article: {
          select: {
            id: true,
          },
        },

        id: true,

        tag: {
          select: {
            id: true,
          },
        },
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: ArticleToTag })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "ArticleToTag",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateArticleToTag(
    @common.Param() params: ArticleToTagWhereUniqueInput,
    @common.Body() data: ArticleToTagUpdateInput
  ): Promise<ArticleToTag | null> {
    try {
      return await this.service.updateArticleToTag({
        where: params,
        data: {
          ...data,

          article: {
            connect: data.article,
          },

          tag: {
            connect: data.tag,
          },
        },
        select: {
          article: {
            select: {
              id: true,
            },
          },

          id: true,

          tag: {
            select: {
              id: true,
            },
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: ArticleToTag })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "ArticleToTag",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteArticleToTag(
    @common.Param() params: ArticleToTagWhereUniqueInput
  ): Promise<ArticleToTag | null> {
    try {
      return await this.service.deleteArticleToTag({
        where: params,
        select: {
          article: {
            select: {
              id: true,
            },
          },

          id: true,

          tag: {
            select: {
              id: true,
            },
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
