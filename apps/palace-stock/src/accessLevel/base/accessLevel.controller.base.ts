/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { AccessLevelService } from "../accessLevel.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { AccessLevelCreateInput } from "./AccessLevelCreateInput";
import { AccessLevel } from "./AccessLevel";
import { AccessLevelFindManyArgs } from "./AccessLevelFindManyArgs";
import { AccessLevelWhereUniqueInput } from "./AccessLevelWhereUniqueInput";
import { AccessLevelUpdateInput } from "./AccessLevelUpdateInput";
import { UserModelFindManyArgs } from "../../userModel/base/UserModelFindManyArgs";
import { UserModel } from "../../userModel/base/UserModel";
import { UserModelWhereUniqueInput } from "../../userModel/base/UserModelWhereUniqueInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class AccessLevelControllerBase {
  constructor(
    protected readonly service: AccessLevelService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: AccessLevel })
  @nestAccessControl.UseRoles({
    resource: "AccessLevel",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createAccessLevel(
    @common.Body() data: AccessLevelCreateInput
  ): Promise<AccessLevel> {
    return await this.service.createAccessLevel({
      data: data,
      select: {
        descripcionNivel: true,
        id: true,
        nivel: true,
        nivelConsulta: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [AccessLevel] })
  @ApiNestedQuery(AccessLevelFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "AccessLevel",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async accessLevels(@common.Req() request: Request): Promise<AccessLevel[]> {
    const args = plainToClass(AccessLevelFindManyArgs, request.query);
    return this.service.accessLevels({
      ...args,
      select: {
        descripcionNivel: true,
        id: true,
        nivel: true,
        nivelConsulta: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: AccessLevel })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "AccessLevel",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async accessLevel(
    @common.Param() params: AccessLevelWhereUniqueInput
  ): Promise<AccessLevel | null> {
    const result = await this.service.accessLevel({
      where: params,
      select: {
        descripcionNivel: true,
        id: true,
        nivel: true,
        nivelConsulta: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: AccessLevel })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "AccessLevel",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateAccessLevel(
    @common.Param() params: AccessLevelWhereUniqueInput,
    @common.Body() data: AccessLevelUpdateInput
  ): Promise<AccessLevel | null> {
    try {
      return await this.service.updateAccessLevel({
        where: params,
        data: data,
        select: {
          descripcionNivel: true,
          id: true,
          nivel: true,
          nivelConsulta: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: AccessLevel })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "AccessLevel",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteAccessLevel(
    @common.Param() params: AccessLevelWhereUniqueInput
  ): Promise<AccessLevel | null> {
    try {
      return await this.service.deleteAccessLevel({
        where: params,
        select: {
          descripcionNivel: true,
          id: true,
          nivel: true,
          nivelConsulta: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id/users")
  @ApiNestedQuery(UserModelFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "UserModel",
    action: "read",
    possession: "any",
  })
  async findUsers(
    @common.Req() request: Request,
    @common.Param() params: AccessLevelWhereUniqueInput
  ): Promise<UserModel[]> {
    const query = plainToClass(UserModelFindManyArgs, request.query);
    const results = await this.service.findUsers(params.id, {
      ...query,
      select: {
        accessLevels: {
          select: {
            id: true,
          },
        },

        collaboratorsUsersCollaboratorIdTocollaborators: {
          select: {
            id: true,
          },
        },

        estatus_usuario: true,
        fechaRegistroUsuario: true,
        id: true,
        idColabRegistroUsuario: true,
        passCifrado: true,
        permisosExcluidos: true,
        userName: true,
      },
    });
    if (results === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return results;
  }

  @common.Post("/:id/users")
  @nestAccessControl.UseRoles({
    resource: "AccessLevel",
    action: "update",
    possession: "any",
  })
  async connectUsers(
    @common.Param() params: AccessLevelWhereUniqueInput,
    @common.Body() body: UserModelWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      users: {
        connect: body,
      },
    };
    await this.service.updateAccessLevel({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Patch("/:id/users")
  @nestAccessControl.UseRoles({
    resource: "AccessLevel",
    action: "update",
    possession: "any",
  })
  async updateUsers(
    @common.Param() params: AccessLevelWhereUniqueInput,
    @common.Body() body: UserModelWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      users: {
        set: body,
      },
    };
    await this.service.updateAccessLevel({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Delete("/:id/users")
  @nestAccessControl.UseRoles({
    resource: "AccessLevel",
    action: "update",
    possession: "any",
  })
  async disconnectUsers(
    @common.Param() params: AccessLevelWhereUniqueInput,
    @common.Body() body: UserModelWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      users: {
        disconnect: body,
      },
    };
    await this.service.updateAccessLevel({
      where: params,
      data,
      select: { id: true },
    });
  }
}
