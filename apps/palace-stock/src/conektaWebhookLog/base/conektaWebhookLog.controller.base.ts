/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { ConektaWebhookLogService } from "../conektaWebhookLog.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { ConektaWebhookLogCreateInput } from "./ConektaWebhookLogCreateInput";
import { ConektaWebhookLog } from "./ConektaWebhookLog";
import { ConektaWebhookLogFindManyArgs } from "./ConektaWebhookLogFindManyArgs";
import { ConektaWebhookLogWhereUniqueInput } from "./ConektaWebhookLogWhereUniqueInput";
import { ConektaWebhookLogUpdateInput } from "./ConektaWebhookLogUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class ConektaWebhookLogControllerBase {
  constructor(
    protected readonly service: ConektaWebhookLogService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: ConektaWebhookLog })
  @nestAccessControl.UseRoles({
    resource: "ConektaWebhookLog",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createConektaWebhookLog(
    @common.Body() data: ConektaWebhookLogCreateInput
  ): Promise<ConektaWebhookLog> {
    return await this.service.createConektaWebhookLog({
      data: data,
      select: {
        createdAt: true,
        eventType: true,
        id: true,
        idWebhook: true,
        response: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [ConektaWebhookLog] })
  @ApiNestedQuery(ConektaWebhookLogFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "ConektaWebhookLog",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async conektaWebhookLogs(
    @common.Req() request: Request
  ): Promise<ConektaWebhookLog[]> {
    const args = plainToClass(ConektaWebhookLogFindManyArgs, request.query);
    return this.service.conektaWebhookLogs({
      ...args,
      select: {
        createdAt: true,
        eventType: true,
        id: true,
        idWebhook: true,
        response: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: ConektaWebhookLog })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "ConektaWebhookLog",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async conektaWebhookLog(
    @common.Param() params: ConektaWebhookLogWhereUniqueInput
  ): Promise<ConektaWebhookLog | null> {
    const result = await this.service.conektaWebhookLog({
      where: params,
      select: {
        createdAt: true,
        eventType: true,
        id: true,
        idWebhook: true,
        response: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: ConektaWebhookLog })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "ConektaWebhookLog",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateConektaWebhookLog(
    @common.Param() params: ConektaWebhookLogWhereUniqueInput,
    @common.Body() data: ConektaWebhookLogUpdateInput
  ): Promise<ConektaWebhookLog | null> {
    try {
      return await this.service.updateConektaWebhookLog({
        where: params,
        data: data,
        select: {
          createdAt: true,
          eventType: true,
          id: true,
          idWebhook: true,
          response: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: ConektaWebhookLog })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "ConektaWebhookLog",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteConektaWebhookLog(
    @common.Param() params: ConektaWebhookLogWhereUniqueInput
  ): Promise<ConektaWebhookLog | null> {
    try {
      return await this.service.deleteConektaWebhookLog({
        where: params,
        select: {
          createdAt: true,
          eventType: true,
          id: true,
          idWebhook: true,
          response: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
