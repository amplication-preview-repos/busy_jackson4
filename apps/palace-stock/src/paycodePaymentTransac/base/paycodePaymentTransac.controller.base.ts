/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { PaycodePaymentTransacService } from "../paycodePaymentTransac.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { PaycodePaymentTransacCreateInput } from "./PaycodePaymentTransacCreateInput";
import { PaycodePaymentTransac } from "./PaycodePaymentTransac";
import { PaycodePaymentTransacFindManyArgs } from "./PaycodePaymentTransacFindManyArgs";
import { PaycodePaymentTransacWhereUniqueInput } from "./PaycodePaymentTransacWhereUniqueInput";
import { PaycodePaymentTransacUpdateInput } from "./PaycodePaymentTransacUpdateInput";
import { AirtimeRechargeFindManyArgs } from "../../airtimeRecharge/base/AirtimeRechargeFindManyArgs";
import { AirtimeRecharge } from "../../airtimeRecharge/base/AirtimeRecharge";
import { AirtimeRechargeWhereUniqueInput } from "../../airtimeRecharge/base/AirtimeRechargeWhereUniqueInput";
import { PaycodeApiLogFindManyArgs } from "../../paycodeApiLog/base/PaycodeApiLogFindManyArgs";
import { PaycodeApiLog } from "../../paycodeApiLog/base/PaycodeApiLog";
import { PaycodeApiLogWhereUniqueInput } from "../../paycodeApiLog/base/PaycodeApiLogWhereUniqueInput";
import { PaycodeWebhookLogFindManyArgs } from "../../paycodeWebhookLog/base/PaycodeWebhookLogFindManyArgs";
import { PaycodeWebhookLog } from "../../paycodeWebhookLog/base/PaycodeWebhookLog";
import { PaycodeWebhookLogWhereUniqueInput } from "../../paycodeWebhookLog/base/PaycodeWebhookLogWhereUniqueInput";
import { PaymentFindManyArgs } from "../../payment/base/PaymentFindManyArgs";
import { Payment } from "../../payment/base/Payment";
import { PaymentWhereUniqueInput } from "../../payment/base/PaymentWhereUniqueInput";
import { RecurringPaymentFindManyArgs } from "../../recurringPayment/base/RecurringPaymentFindManyArgs";
import { RecurringPayment } from "../../recurringPayment/base/RecurringPayment";
import { RecurringPaymentWhereUniqueInput } from "../../recurringPayment/base/RecurringPaymentWhereUniqueInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class PaycodePaymentTransacControllerBase {
  constructor(
    protected readonly service: PaycodePaymentTransacService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: PaycodePaymentTransac })
  @nestAccessControl.UseRoles({
    resource: "PaycodePaymentTransac",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createPaycodePaymentTransac(
    @common.Body() data: PaycodePaymentTransacCreateInput
  ): Promise<PaycodePaymentTransac> {
    return await this.service.createPaycodePaymentTransac({
      data: {
        ...data,

        financedSales: {
          connect: data.financedSales,
        },
      },
      select: {
        bank: true,
        beneficiary: true,
        clabe: true,
        concept: true,
        datosPago: true,
        displayMessage: true,
        estatusCobro: true,
        fechaRegistro: true,

        financedSales: {
          select: {
            id: true,
          },
        },

        habilitarPagoRecurrente: true,
        id: true,
        referenceNumber: true,
        trackCode: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [PaycodePaymentTransac] })
  @ApiNestedQuery(PaycodePaymentTransacFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "PaycodePaymentTransac",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async paycodePaymentTransacs(
    @common.Req() request: Request
  ): Promise<PaycodePaymentTransac[]> {
    const args = plainToClass(PaycodePaymentTransacFindManyArgs, request.query);
    return this.service.paycodePaymentTransacs({
      ...args,
      select: {
        bank: true,
        beneficiary: true,
        clabe: true,
        concept: true,
        datosPago: true,
        displayMessage: true,
        estatusCobro: true,
        fechaRegistro: true,

        financedSales: {
          select: {
            id: true,
          },
        },

        habilitarPagoRecurrente: true,
        id: true,
        referenceNumber: true,
        trackCode: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: PaycodePaymentTransac })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "PaycodePaymentTransac",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async paycodePaymentTransac(
    @common.Param() params: PaycodePaymentTransacWhereUniqueInput
  ): Promise<PaycodePaymentTransac | null> {
    const result = await this.service.paycodePaymentTransac({
      where: params,
      select: {
        bank: true,
        beneficiary: true,
        clabe: true,
        concept: true,
        datosPago: true,
        displayMessage: true,
        estatusCobro: true,
        fechaRegistro: true,

        financedSales: {
          select: {
            id: true,
          },
        },

        habilitarPagoRecurrente: true,
        id: true,
        referenceNumber: true,
        trackCode: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: PaycodePaymentTransac })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "PaycodePaymentTransac",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updatePaycodePaymentTransac(
    @common.Param() params: PaycodePaymentTransacWhereUniqueInput,
    @common.Body() data: PaycodePaymentTransacUpdateInput
  ): Promise<PaycodePaymentTransac | null> {
    try {
      return await this.service.updatePaycodePaymentTransac({
        where: params,
        data: {
          ...data,

          financedSales: {
            connect: data.financedSales,
          },
        },
        select: {
          bank: true,
          beneficiary: true,
          clabe: true,
          concept: true,
          datosPago: true,
          displayMessage: true,
          estatusCobro: true,
          fechaRegistro: true,

          financedSales: {
            select: {
              id: true,
            },
          },

          habilitarPagoRecurrente: true,
          id: true,
          referenceNumber: true,
          trackCode: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: PaycodePaymentTransac })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "PaycodePaymentTransac",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deletePaycodePaymentTransac(
    @common.Param() params: PaycodePaymentTransacWhereUniqueInput
  ): Promise<PaycodePaymentTransac | null> {
    try {
      return await this.service.deletePaycodePaymentTransac({
        where: params,
        select: {
          bank: true,
          beneficiary: true,
          clabe: true,
          concept: true,
          datosPago: true,
          displayMessage: true,
          estatusCobro: true,
          fechaRegistro: true,

          financedSales: {
            select: {
              id: true,
            },
          },

          habilitarPagoRecurrente: true,
          id: true,
          referenceNumber: true,
          trackCode: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id/airtimeRecharges")
  @ApiNestedQuery(AirtimeRechargeFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "AirtimeRecharge",
    action: "read",
    possession: "any",
  })
  async findAirtimeRecharges(
    @common.Req() request: Request,
    @common.Param() params: PaycodePaymentTransacWhereUniqueInput
  ): Promise<AirtimeRecharge[]> {
    const query = plainToClass(AirtimeRechargeFindManyArgs, request.query);
    const results = await this.service.findAirtimeRecharges(params.id, {
      ...query,
      select: {
        conektaPaymentTransacs: {
          select: {
            id: true,
          },
        },

        descripcion: true,
        fechaRegistro: true,
        id: true,
        idTransaccion: true,
        monto: true,
        numeroTel: true,
        operadora: true,

        paycodePaymentTransacs: {
          select: {
            id: true,
          },
        },

        sku: true,
        tipo: true,
        vigencia: true,
      },
    });
    if (results === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return results;
  }

  @common.Post("/:id/airtimeRecharges")
  @nestAccessControl.UseRoles({
    resource: "PaycodePaymentTransac",
    action: "update",
    possession: "any",
  })
  async connectAirtimeRecharges(
    @common.Param() params: PaycodePaymentTransacWhereUniqueInput,
    @common.Body() body: AirtimeRechargeWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      airtimeRecharges: {
        connect: body,
      },
    };
    await this.service.updatePaycodePaymentTransac({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Patch("/:id/airtimeRecharges")
  @nestAccessControl.UseRoles({
    resource: "PaycodePaymentTransac",
    action: "update",
    possession: "any",
  })
  async updateAirtimeRecharges(
    @common.Param() params: PaycodePaymentTransacWhereUniqueInput,
    @common.Body() body: AirtimeRechargeWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      airtimeRecharges: {
        set: body,
      },
    };
    await this.service.updatePaycodePaymentTransac({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Delete("/:id/airtimeRecharges")
  @nestAccessControl.UseRoles({
    resource: "PaycodePaymentTransac",
    action: "update",
    possession: "any",
  })
  async disconnectAirtimeRecharges(
    @common.Param() params: PaycodePaymentTransacWhereUniqueInput,
    @common.Body() body: AirtimeRechargeWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      airtimeRecharges: {
        disconnect: body,
      },
    };
    await this.service.updatePaycodePaymentTransac({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id/paycodeApiLog")
  @ApiNestedQuery(PaycodeApiLogFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "PaycodeApiLog",
    action: "read",
    possession: "any",
  })
  async findPaycodeApiLog(
    @common.Req() request: Request,
    @common.Param() params: PaycodePaymentTransacWhereUniqueInput
  ): Promise<PaycodeApiLog[]> {
    const query = plainToClass(PaycodeApiLogFindManyArgs, request.query);
    const results = await this.service.findPaycodeApiLog(params.id, {
      ...query,
      select: {
        callDate: true,
        calledMethod: true,
        callTime: true,
        calltrace: true,
        dataSend: true,
        headers: true,
        id: true,

        paycodePaymentTransacs: {
          select: {
            id: true,
          },
        },

        response: true,
        responseDatetime: true,
        url: true,
      },
    });
    if (results === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return results;
  }

  @common.Post("/:id/paycodeApiLog")
  @nestAccessControl.UseRoles({
    resource: "PaycodePaymentTransac",
    action: "update",
    possession: "any",
  })
  async connectPaycodeApiLog(
    @common.Param() params: PaycodePaymentTransacWhereUniqueInput,
    @common.Body() body: PaycodeApiLogWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      paycodeApiLog: {
        connect: body,
      },
    };
    await this.service.updatePaycodePaymentTransac({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Patch("/:id/paycodeApiLog")
  @nestAccessControl.UseRoles({
    resource: "PaycodePaymentTransac",
    action: "update",
    possession: "any",
  })
  async updatePaycodeApiLog(
    @common.Param() params: PaycodePaymentTransacWhereUniqueInput,
    @common.Body() body: PaycodeApiLogWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      paycodeApiLog: {
        set: body,
      },
    };
    await this.service.updatePaycodePaymentTransac({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Delete("/:id/paycodeApiLog")
  @nestAccessControl.UseRoles({
    resource: "PaycodePaymentTransac",
    action: "update",
    possession: "any",
  })
  async disconnectPaycodeApiLog(
    @common.Param() params: PaycodePaymentTransacWhereUniqueInput,
    @common.Body() body: PaycodeApiLogWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      paycodeApiLog: {
        disconnect: body,
      },
    };
    await this.service.updatePaycodePaymentTransac({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id/paycodeWebhookLog")
  @ApiNestedQuery(PaycodeWebhookLogFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "PaycodeWebhookLog",
    action: "read",
    possession: "any",
  })
  async findPaycodeWebhookLog(
    @common.Req() request: Request,
    @common.Param() params: PaycodePaymentTransacWhereUniqueInput
  ): Promise<PaycodeWebhookLog[]> {
    const query = plainToClass(PaycodeWebhookLogFindManyArgs, request.query);
    const results = await this.service.findPaycodeWebhookLog(params.id, {
      ...query,
      select: {
        fechaRegistro: true,
        id: true,

        paycodePaymentTransacs: {
          select: {
            id: true,
          },
        },

        response: true,
      },
    });
    if (results === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return results;
  }

  @common.Post("/:id/paycodeWebhookLog")
  @nestAccessControl.UseRoles({
    resource: "PaycodePaymentTransac",
    action: "update",
    possession: "any",
  })
  async connectPaycodeWebhookLog(
    @common.Param() params: PaycodePaymentTransacWhereUniqueInput,
    @common.Body() body: PaycodeWebhookLogWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      paycodeWebhookLog: {
        connect: body,
      },
    };
    await this.service.updatePaycodePaymentTransac({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Patch("/:id/paycodeWebhookLog")
  @nestAccessControl.UseRoles({
    resource: "PaycodePaymentTransac",
    action: "update",
    possession: "any",
  })
  async updatePaycodeWebhookLog(
    @common.Param() params: PaycodePaymentTransacWhereUniqueInput,
    @common.Body() body: PaycodeWebhookLogWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      paycodeWebhookLog: {
        set: body,
      },
    };
    await this.service.updatePaycodePaymentTransac({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Delete("/:id/paycodeWebhookLog")
  @nestAccessControl.UseRoles({
    resource: "PaycodePaymentTransac",
    action: "update",
    possession: "any",
  })
  async disconnectPaycodeWebhookLog(
    @common.Param() params: PaycodePaymentTransacWhereUniqueInput,
    @common.Body() body: PaycodeWebhookLogWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      paycodeWebhookLog: {
        disconnect: body,
      },
    };
    await this.service.updatePaycodePaymentTransac({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id/payments")
  @ApiNestedQuery(PaymentFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "Payment",
    action: "read",
    possession: "any",
  })
  async findPayments(
    @common.Req() request: Request,
    @common.Param() params: PaycodePaymentTransacWhereUniqueInput
  ): Promise<Payment[]> {
    const query = plainToClass(PaymentFindManyArgs, request.query);
    const results = await this.service.findPayments(params.id, {
      ...query,
      select: {
        abonoExtra: true,
        cambioDevuelto: true,
        canceledUserId: true,
        cantidadRecibida: true,
        capitalPagado: true,

        cashCountDetails: {
          select: {
            id: true,
          },
        },

        comentariosPago: true,
        comprobantePago: true,

        conektaPaymentTransacs: {
          select: {
            id: true,
          },
        },

        customerId: true,
        desctoAplicado: true,
        desdeWebhook: true,
        fechaCancelo: true,
        fechaPago: true,
        fechaProxVenc: true,
        fechaVenc: true,

        financedSales: {
          select: {
            id: true,
          },
        },

        formaPago: true,
        horaPago: true,
        id: true,
        interesPagado: true,
        opcionCambio: true,
        opcionPago: true,
        pagoRecurrente: true,
        pagosAdelantados: true,
        pathFileDiscount: true,

        paycodePaymentTransacs: {
          select: {
            id: true,
          },
        },

        payCount: true,
        pay_status: true,
        pay_type: true,
        percentageDiscountDownPayment: true,

        personalLoans: {
          select: {
            id: true,
          },
        },

        promotionId: true,
        receivedUserId: true,

        stripePaymentTransacs: {
          select: {
            id: true,
          },
        },

        warehouses: {
          select: {
            id: true,
          },
        },
      },
    });
    if (results === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return results;
  }

  @common.Post("/:id/payments")
  @nestAccessControl.UseRoles({
    resource: "PaycodePaymentTransac",
    action: "update",
    possession: "any",
  })
  async connectPayments(
    @common.Param() params: PaycodePaymentTransacWhereUniqueInput,
    @common.Body() body: PaymentWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      payments: {
        connect: body,
      },
    };
    await this.service.updatePaycodePaymentTransac({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Patch("/:id/payments")
  @nestAccessControl.UseRoles({
    resource: "PaycodePaymentTransac",
    action: "update",
    possession: "any",
  })
  async updatePayments(
    @common.Param() params: PaycodePaymentTransacWhereUniqueInput,
    @common.Body() body: PaymentWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      payments: {
        set: body,
      },
    };
    await this.service.updatePaycodePaymentTransac({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Delete("/:id/payments")
  @nestAccessControl.UseRoles({
    resource: "PaycodePaymentTransac",
    action: "update",
    possession: "any",
  })
  async disconnectPayments(
    @common.Param() params: PaycodePaymentTransacWhereUniqueInput,
    @common.Body() body: PaymentWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      payments: {
        disconnect: body,
      },
    };
    await this.service.updatePaycodePaymentTransac({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id/recurringPayments")
  @ApiNestedQuery(RecurringPaymentFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "RecurringPayment",
    action: "read",
    possession: "any",
  })
  async findRecurringPayments(
    @common.Req() request: Request,
    @common.Param() params: PaycodePaymentTransacWhereUniqueInput
  ): Promise<RecurringPayment[]> {
    const query = plainToClass(RecurringPaymentFindManyArgs, request.query);
    const results = await this.service.findRecurringPayments(params.id, {
      ...query,
      select: {
        conektaPaymentTransacs: {
          select: {
            id: true,
          },
        },

        estatus_intento: true,
        fechaRegistro: true,

        financedSales: {
          select: {
            id: true,
          },
        },

        horaRegistro: true,
        id: true,
        mensajeIntento: true,

        paycodePaymentTransacs: {
          select: {
            id: true,
          },
        },

        stripePaymentTransacs: {
          select: {
            id: true,
          },
        },
      },
    });
    if (results === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return results;
  }

  @common.Post("/:id/recurringPayments")
  @nestAccessControl.UseRoles({
    resource: "PaycodePaymentTransac",
    action: "update",
    possession: "any",
  })
  async connectRecurringPayments(
    @common.Param() params: PaycodePaymentTransacWhereUniqueInput,
    @common.Body() body: RecurringPaymentWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      recurringPayments: {
        connect: body,
      },
    };
    await this.service.updatePaycodePaymentTransac({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Patch("/:id/recurringPayments")
  @nestAccessControl.UseRoles({
    resource: "PaycodePaymentTransac",
    action: "update",
    possession: "any",
  })
  async updateRecurringPayments(
    @common.Param() params: PaycodePaymentTransacWhereUniqueInput,
    @common.Body() body: RecurringPaymentWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      recurringPayments: {
        set: body,
      },
    };
    await this.service.updatePaycodePaymentTransac({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Delete("/:id/recurringPayments")
  @nestAccessControl.UseRoles({
    resource: "PaycodePaymentTransac",
    action: "update",
    possession: "any",
  })
  async disconnectRecurringPayments(
    @common.Param() params: PaycodePaymentTransacWhereUniqueInput,
    @common.Body() body: RecurringPaymentWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      recurringPayments: {
        disconnect: body,
      },
    };
    await this.service.updatePaycodePaymentTransac({
      where: params,
      data,
      select: { id: true },
    });
  }
}
