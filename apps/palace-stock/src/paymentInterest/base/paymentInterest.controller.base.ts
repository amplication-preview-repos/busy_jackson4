/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { PaymentInterestService } from "../paymentInterest.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { PaymentInterestCreateInput } from "./PaymentInterestCreateInput";
import { PaymentInterest } from "./PaymentInterest";
import { PaymentInterestFindManyArgs } from "./PaymentInterestFindManyArgs";
import { PaymentInterestWhereUniqueInput } from "./PaymentInterestWhereUniqueInput";
import { PaymentInterestUpdateInput } from "./PaymentInterestUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class PaymentInterestControllerBase {
  constructor(
    protected readonly service: PaymentInterestService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: PaymentInterest })
  @nestAccessControl.UseRoles({
    resource: "PaymentInterest",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createPaymentInterest(
    @common.Body() data: PaymentInterestCreateInput
  ): Promise<PaymentInterest> {
    return await this.service.createPaymentInterest({
      data: {
        ...data,

        warehouses: data.warehouses
          ? {
              connect: data.warehouses,
            }
          : undefined,
      },
      select: {
        id: true,
        interesAnual: true,
        interesSemanal: true,
        interesSemanalMultiplo: true,
        paymentPeriodId: true,
        rangoFinal: true,
        rangoInicial: true,

        warehouses: {
          select: {
            id: true,
          },
        },
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [PaymentInterest] })
  @ApiNestedQuery(PaymentInterestFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "PaymentInterest",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async paymentInterests(
    @common.Req() request: Request
  ): Promise<PaymentInterest[]> {
    const args = plainToClass(PaymentInterestFindManyArgs, request.query);
    return this.service.paymentInterests({
      ...args,
      select: {
        id: true,
        interesAnual: true,
        interesSemanal: true,
        interesSemanalMultiplo: true,
        paymentPeriodId: true,
        rangoFinal: true,
        rangoInicial: true,

        warehouses: {
          select: {
            id: true,
          },
        },
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: PaymentInterest })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "PaymentInterest",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async paymentInterest(
    @common.Param() params: PaymentInterestWhereUniqueInput
  ): Promise<PaymentInterest | null> {
    const result = await this.service.paymentInterest({
      where: params,
      select: {
        id: true,
        interesAnual: true,
        interesSemanal: true,
        interesSemanalMultiplo: true,
        paymentPeriodId: true,
        rangoFinal: true,
        rangoInicial: true,

        warehouses: {
          select: {
            id: true,
          },
        },
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: PaymentInterest })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "PaymentInterest",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updatePaymentInterest(
    @common.Param() params: PaymentInterestWhereUniqueInput,
    @common.Body() data: PaymentInterestUpdateInput
  ): Promise<PaymentInterest | null> {
    try {
      return await this.service.updatePaymentInterest({
        where: params,
        data: {
          ...data,

          warehouses: data.warehouses
            ? {
                connect: data.warehouses,
              }
            : undefined,
        },
        select: {
          id: true,
          interesAnual: true,
          interesSemanal: true,
          interesSemanalMultiplo: true,
          paymentPeriodId: true,
          rangoFinal: true,
          rangoInicial: true,

          warehouses: {
            select: {
              id: true,
            },
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: PaymentInterest })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "PaymentInterest",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deletePaymentInterest(
    @common.Param() params: PaymentInterestWhereUniqueInput
  ): Promise<PaymentInterest | null> {
    try {
      return await this.service.deletePaymentInterest({
        where: params,
        select: {
          id: true,
          interesAnual: true,
          interesSemanal: true,
          interesSemanalMultiplo: true,
          paymentPeriodId: true,
          rangoFinal: true,
          rangoInicial: true,

          warehouses: {
            select: {
              id: true,
            },
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
