/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { LockedDevBadCustomerService } from "../lockedDevBadCustomer.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { LockedDevBadCustomerCreateInput } from "./LockedDevBadCustomerCreateInput";
import { LockedDevBadCustomer } from "./LockedDevBadCustomer";
import { LockedDevBadCustomerFindManyArgs } from "./LockedDevBadCustomerFindManyArgs";
import { LockedDevBadCustomerWhereUniqueInput } from "./LockedDevBadCustomerWhereUniqueInput";
import { LockedDevBadCustomerUpdateInput } from "./LockedDevBadCustomerUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class LockedDevBadCustomerControllerBase {
  constructor(
    protected readonly service: LockedDevBadCustomerService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: LockedDevBadCustomer })
  @nestAccessControl.UseRoles({
    resource: "LockedDevBadCustomer",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createLockedDevBadCustomer(
    @common.Body() data: LockedDevBadCustomerCreateInput
  ): Promise<LockedDevBadCustomer> {
    return await this.service.createLockedDevBadCustomer({
      data: {
        ...data,

        customers: data.customers
          ? {
              connect: data.customers,
            }
          : undefined,

        financedSales: data.financedSales
          ? {
              connect: data.financedSales,
            }
          : undefined,
      },
      select: {
        codigoBloqueoMoroso: true,

        customers: {
          select: {
            id: true,
          },
        },

        deviceId: true,
        fechaBloqueado: true,
        fechaDesbloqueado: true,

        financedSales: {
          select: {
            id: true,
          },
        },

        forced_status: true,
        id: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [LockedDevBadCustomer] })
  @ApiNestedQuery(LockedDevBadCustomerFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "LockedDevBadCustomer",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async lockedDevBadCustomers(
    @common.Req() request: Request
  ): Promise<LockedDevBadCustomer[]> {
    const args = plainToClass(LockedDevBadCustomerFindManyArgs, request.query);
    return this.service.lockedDevBadCustomers({
      ...args,
      select: {
        codigoBloqueoMoroso: true,

        customers: {
          select: {
            id: true,
          },
        },

        deviceId: true,
        fechaBloqueado: true,
        fechaDesbloqueado: true,

        financedSales: {
          select: {
            id: true,
          },
        },

        forced_status: true,
        id: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: LockedDevBadCustomer })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "LockedDevBadCustomer",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async lockedDevBadCustomer(
    @common.Param() params: LockedDevBadCustomerWhereUniqueInput
  ): Promise<LockedDevBadCustomer | null> {
    const result = await this.service.lockedDevBadCustomer({
      where: params,
      select: {
        codigoBloqueoMoroso: true,

        customers: {
          select: {
            id: true,
          },
        },

        deviceId: true,
        fechaBloqueado: true,
        fechaDesbloqueado: true,

        financedSales: {
          select: {
            id: true,
          },
        },

        forced_status: true,
        id: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: LockedDevBadCustomer })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "LockedDevBadCustomer",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateLockedDevBadCustomer(
    @common.Param() params: LockedDevBadCustomerWhereUniqueInput,
    @common.Body() data: LockedDevBadCustomerUpdateInput
  ): Promise<LockedDevBadCustomer | null> {
    try {
      return await this.service.updateLockedDevBadCustomer({
        where: params,
        data: {
          ...data,

          customers: data.customers
            ? {
                connect: data.customers,
              }
            : undefined,

          financedSales: data.financedSales
            ? {
                connect: data.financedSales,
              }
            : undefined,
        },
        select: {
          codigoBloqueoMoroso: true,

          customers: {
            select: {
              id: true,
            },
          },

          deviceId: true,
          fechaBloqueado: true,
          fechaDesbloqueado: true,

          financedSales: {
            select: {
              id: true,
            },
          },

          forced_status: true,
          id: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: LockedDevBadCustomer })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "LockedDevBadCustomer",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteLockedDevBadCustomer(
    @common.Param() params: LockedDevBadCustomerWhereUniqueInput
  ): Promise<LockedDevBadCustomer | null> {
    try {
      return await this.service.deleteLockedDevBadCustomer({
        where: params,
        select: {
          codigoBloqueoMoroso: true,

          customers: {
            select: {
              id: true,
            },
          },

          deviceId: true,
          fechaBloqueado: true,
          fechaDesbloqueado: true,

          financedSales: {
            select: {
              id: true,
            },
          },

          forced_status: true,
          id: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
