/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { TransacErrorService } from "../transacError.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { TransacErrorCreateInput } from "./TransacErrorCreateInput";
import { TransacError } from "./TransacError";
import { TransacErrorFindManyArgs } from "./TransacErrorFindManyArgs";
import { TransacErrorWhereUniqueInput } from "./TransacErrorWhereUniqueInput";
import { TransacErrorUpdateInput } from "./TransacErrorUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class TransacErrorControllerBase {
  constructor(
    protected readonly service: TransacErrorService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: TransacError })
  @nestAccessControl.UseRoles({
    resource: "TransacError",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createTransacError(
    @common.Body() data: TransacErrorCreateInput
  ): Promise<TransacError> {
    return await this.service.createTransacError({
      data: {
        ...data,

        branchOffices: {
          connect: data.branchOffices,
        },
      },
      select: {
        branchOffices: {
          select: {
            id: true,
          },
        },

        folioTrans: true,
        id: true,
        seriesError: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [TransacError] })
  @ApiNestedQuery(TransacErrorFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "TransacError",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async transacErrors(@common.Req() request: Request): Promise<TransacError[]> {
    const args = plainToClass(TransacErrorFindManyArgs, request.query);
    return this.service.transacErrors({
      ...args,
      select: {
        branchOffices: {
          select: {
            id: true,
          },
        },

        folioTrans: true,
        id: true,
        seriesError: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: TransacError })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "TransacError",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async transacError(
    @common.Param() params: TransacErrorWhereUniqueInput
  ): Promise<TransacError | null> {
    const result = await this.service.transacError({
      where: params,
      select: {
        branchOffices: {
          select: {
            id: true,
          },
        },

        folioTrans: true,
        id: true,
        seriesError: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: TransacError })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "TransacError",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateTransacError(
    @common.Param() params: TransacErrorWhereUniqueInput,
    @common.Body() data: TransacErrorUpdateInput
  ): Promise<TransacError | null> {
    try {
      return await this.service.updateTransacError({
        where: params,
        data: {
          ...data,

          branchOffices: {
            connect: data.branchOffices,
          },
        },
        select: {
          branchOffices: {
            select: {
              id: true,
            },
          },

          folioTrans: true,
          id: true,
          seriesError: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: TransacError })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "TransacError",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteTransacError(
    @common.Param() params: TransacErrorWhereUniqueInput
  ): Promise<TransacError | null> {
    try {
      return await this.service.deleteTransacError({
        where: params,
        select: {
          branchOffices: {
            select: {
              id: true,
            },
          },

          folioTrans: true,
          id: true,
          seriesError: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
