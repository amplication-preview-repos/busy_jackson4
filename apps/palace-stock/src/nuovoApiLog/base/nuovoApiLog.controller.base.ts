/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { NuovoApiLogService } from "../nuovoApiLog.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { NuovoApiLogCreateInput } from "./NuovoApiLogCreateInput";
import { NuovoApiLog } from "./NuovoApiLog";
import { NuovoApiLogFindManyArgs } from "./NuovoApiLogFindManyArgs";
import { NuovoApiLogWhereUniqueInput } from "./NuovoApiLogWhereUniqueInput";
import { NuovoApiLogUpdateInput } from "./NuovoApiLogUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class NuovoApiLogControllerBase {
  constructor(
    protected readonly service: NuovoApiLogService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: NuovoApiLog })
  @nestAccessControl.UseRoles({
    resource: "NuovoApiLog",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createNuovoApiLog(
    @common.Body() data: NuovoApiLogCreateInput
  ): Promise<NuovoApiLog> {
    return await this.service.createNuovoApiLog({
      data: data,
      select: {
        callDate: true,
        calledMethod: true,
        callTime: true,
        calltrace: true,
        dataSend: true,
        headers: true,
        id: true,
        response: true,
        responseDatetime: true,
        url: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [NuovoApiLog] })
  @ApiNestedQuery(NuovoApiLogFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "NuovoApiLog",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async nuovoApiLogs(@common.Req() request: Request): Promise<NuovoApiLog[]> {
    const args = plainToClass(NuovoApiLogFindManyArgs, request.query);
    return this.service.nuovoApiLogs({
      ...args,
      select: {
        callDate: true,
        calledMethod: true,
        callTime: true,
        calltrace: true,
        dataSend: true,
        headers: true,
        id: true,
        response: true,
        responseDatetime: true,
        url: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: NuovoApiLog })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "NuovoApiLog",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async nuovoApiLog(
    @common.Param() params: NuovoApiLogWhereUniqueInput
  ): Promise<NuovoApiLog | null> {
    const result = await this.service.nuovoApiLog({
      where: params,
      select: {
        callDate: true,
        calledMethod: true,
        callTime: true,
        calltrace: true,
        dataSend: true,
        headers: true,
        id: true,
        response: true,
        responseDatetime: true,
        url: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: NuovoApiLog })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "NuovoApiLog",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateNuovoApiLog(
    @common.Param() params: NuovoApiLogWhereUniqueInput,
    @common.Body() data: NuovoApiLogUpdateInput
  ): Promise<NuovoApiLog | null> {
    try {
      return await this.service.updateNuovoApiLog({
        where: params,
        data: data,
        select: {
          callDate: true,
          calledMethod: true,
          callTime: true,
          calltrace: true,
          dataSend: true,
          headers: true,
          id: true,
          response: true,
          responseDatetime: true,
          url: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: NuovoApiLog })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "NuovoApiLog",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteNuovoApiLog(
    @common.Param() params: NuovoApiLogWhereUniqueInput
  ): Promise<NuovoApiLog | null> {
    try {
      return await this.service.deleteNuovoApiLog({
        where: params,
        select: {
          callDate: true,
          calledMethod: true,
          callTime: true,
          calltrace: true,
          dataSend: true,
          headers: true,
          id: true,
          response: true,
          responseDatetime: true,
          url: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
