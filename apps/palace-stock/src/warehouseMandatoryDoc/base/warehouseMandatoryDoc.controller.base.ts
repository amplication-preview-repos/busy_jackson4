/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { WarehouseMandatoryDocService } from "../warehouseMandatoryDoc.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { WarehouseMandatoryDocCreateInput } from "./WarehouseMandatoryDocCreateInput";
import { WarehouseMandatoryDoc } from "./WarehouseMandatoryDoc";
import { WarehouseMandatoryDocFindManyArgs } from "./WarehouseMandatoryDocFindManyArgs";
import { WarehouseMandatoryDocWhereUniqueInput } from "./WarehouseMandatoryDocWhereUniqueInput";
import { WarehouseMandatoryDocUpdateInput } from "./WarehouseMandatoryDocUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class WarehouseMandatoryDocControllerBase {
  constructor(
    protected readonly service: WarehouseMandatoryDocService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: WarehouseMandatoryDoc })
  @nestAccessControl.UseRoles({
    resource: "WarehouseMandatoryDoc",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createWarehouseMandatoryDoc(
    @common.Body() data: WarehouseMandatoryDocCreateInput
  ): Promise<WarehouseMandatoryDoc> {
    return await this.service.createWarehouseMandatoryDoc({
      data: {
        ...data,

        mandatoryDocuments: {
          connect: data.mandatoryDocuments,
        },

        warehouses: {
          connect: data.warehouses,
        },
      },
      select: {
        id: true,

        mandatoryDocuments: {
          select: {
            id: true,
          },
        },

        warehouses: {
          select: {
            id: true,
          },
        },
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [WarehouseMandatoryDoc] })
  @ApiNestedQuery(WarehouseMandatoryDocFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "WarehouseMandatoryDoc",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async warehouseMandatoryDocs(
    @common.Req() request: Request
  ): Promise<WarehouseMandatoryDoc[]> {
    const args = plainToClass(WarehouseMandatoryDocFindManyArgs, request.query);
    return this.service.warehouseMandatoryDocs({
      ...args,
      select: {
        id: true,

        mandatoryDocuments: {
          select: {
            id: true,
          },
        },

        warehouses: {
          select: {
            id: true,
          },
        },
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: WarehouseMandatoryDoc })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "WarehouseMandatoryDoc",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async warehouseMandatoryDoc(
    @common.Param() params: WarehouseMandatoryDocWhereUniqueInput
  ): Promise<WarehouseMandatoryDoc | null> {
    const result = await this.service.warehouseMandatoryDoc({
      where: params,
      select: {
        id: true,

        mandatoryDocuments: {
          select: {
            id: true,
          },
        },

        warehouses: {
          select: {
            id: true,
          },
        },
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: WarehouseMandatoryDoc })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "WarehouseMandatoryDoc",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateWarehouseMandatoryDoc(
    @common.Param() params: WarehouseMandatoryDocWhereUniqueInput,
    @common.Body() data: WarehouseMandatoryDocUpdateInput
  ): Promise<WarehouseMandatoryDoc | null> {
    try {
      return await this.service.updateWarehouseMandatoryDoc({
        where: params,
        data: {
          ...data,

          mandatoryDocuments: {
            connect: data.mandatoryDocuments,
          },

          warehouses: {
            connect: data.warehouses,
          },
        },
        select: {
          id: true,

          mandatoryDocuments: {
            select: {
              id: true,
            },
          },

          warehouses: {
            select: {
              id: true,
            },
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: WarehouseMandatoryDoc })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "WarehouseMandatoryDoc",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteWarehouseMandatoryDoc(
    @common.Param() params: WarehouseMandatoryDocWhereUniqueInput
  ): Promise<WarehouseMandatoryDoc | null> {
    try {
      return await this.service.deleteWarehouseMandatoryDoc({
        where: params,
        select: {
          id: true,

          mandatoryDocuments: {
            select: {
              id: true,
            },
          },

          warehouses: {
            select: {
              id: true,
            },
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
