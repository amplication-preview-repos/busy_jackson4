/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { ModifiedPermissionService } from "../modifiedPermission.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { ModifiedPermissionCreateInput } from "./ModifiedPermissionCreateInput";
import { ModifiedPermission } from "./ModifiedPermission";
import { ModifiedPermissionFindManyArgs } from "./ModifiedPermissionFindManyArgs";
import { ModifiedPermissionWhereUniqueInput } from "./ModifiedPermissionWhereUniqueInput";
import { ModifiedPermissionUpdateInput } from "./ModifiedPermissionUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class ModifiedPermissionControllerBase {
  constructor(
    protected readonly service: ModifiedPermissionService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: ModifiedPermission })
  @nestAccessControl.UseRoles({
    resource: "ModifiedPermission",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createModifiedPermission(
    @common.Body() data: ModifiedPermissionCreateInput
  ): Promise<ModifiedPermission> {
    return await this.service.createModifiedPermission({
      data: {
        ...data,

        permissions: {
          connect: data.permissions,
        },

        users: {
          connect: data.users,
        },
      },
      select: {
        accion: true,
        createdUserId: true,
        fechaRegistro: true,
        id: true,

        permissions: {
          select: {
            id: true,
          },
        },

        users: {
          select: {
            id: true,
          },
        },
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [ModifiedPermission] })
  @ApiNestedQuery(ModifiedPermissionFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "ModifiedPermission",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async modifiedPermissions(
    @common.Req() request: Request
  ): Promise<ModifiedPermission[]> {
    const args = plainToClass(ModifiedPermissionFindManyArgs, request.query);
    return this.service.modifiedPermissions({
      ...args,
      select: {
        accion: true,
        createdUserId: true,
        fechaRegistro: true,
        id: true,

        permissions: {
          select: {
            id: true,
          },
        },

        users: {
          select: {
            id: true,
          },
        },
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: ModifiedPermission })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "ModifiedPermission",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async modifiedPermission(
    @common.Param() params: ModifiedPermissionWhereUniqueInput
  ): Promise<ModifiedPermission | null> {
    const result = await this.service.modifiedPermission({
      where: params,
      select: {
        accion: true,
        createdUserId: true,
        fechaRegistro: true,
        id: true,

        permissions: {
          select: {
            id: true,
          },
        },

        users: {
          select: {
            id: true,
          },
        },
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: ModifiedPermission })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "ModifiedPermission",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateModifiedPermission(
    @common.Param() params: ModifiedPermissionWhereUniqueInput,
    @common.Body() data: ModifiedPermissionUpdateInput
  ): Promise<ModifiedPermission | null> {
    try {
      return await this.service.updateModifiedPermission({
        where: params,
        data: {
          ...data,

          permissions: {
            connect: data.permissions,
          },

          users: {
            connect: data.users,
          },
        },
        select: {
          accion: true,
          createdUserId: true,
          fechaRegistro: true,
          id: true,

          permissions: {
            select: {
              id: true,
            },
          },

          users: {
            select: {
              id: true,
            },
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: ModifiedPermission })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "ModifiedPermission",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteModifiedPermission(
    @common.Param() params: ModifiedPermissionWhereUniqueInput
  ): Promise<ModifiedPermission | null> {
    try {
      return await this.service.deleteModifiedPermission({
        where: params,
        select: {
          accion: true,
          createdUserId: true,
          fechaRegistro: true,
          id: true,

          permissions: {
            select: {
              id: true,
            },
          },

          users: {
            select: {
              id: true,
            },
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
