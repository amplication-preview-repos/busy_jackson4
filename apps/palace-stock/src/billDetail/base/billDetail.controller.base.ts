/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { BillDetailService } from "../billDetail.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { BillDetailCreateInput } from "./BillDetailCreateInput";
import { BillDetail } from "./BillDetail";
import { BillDetailFindManyArgs } from "./BillDetailFindManyArgs";
import { BillDetailWhereUniqueInput } from "./BillDetailWhereUniqueInput";
import { BillDetailUpdateInput } from "./BillDetailUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class BillDetailControllerBase {
  constructor(
    protected readonly service: BillDetailService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: BillDetail })
  @nestAccessControl.UseRoles({
    resource: "BillDetail",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createBillDetail(
    @common.Body() data: BillDetailCreateInput
  ): Promise<BillDetail> {
    return await this.service.createBillDetail({
      data: {
        ...data,

        items: {
          connect: data.items,
        },
      },
      select: {
        codigoSerie: true,
        createdUserId: true,
        desctoUnidad: true,
        fechaRegistro: true,
        id: true,
        idFactura: true,

        items: {
          select: {
            id: true,
          },
        },

        precioUnidad: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [BillDetail] })
  @ApiNestedQuery(BillDetailFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "BillDetail",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async billDetails(@common.Req() request: Request): Promise<BillDetail[]> {
    const args = plainToClass(BillDetailFindManyArgs, request.query);
    return this.service.billDetails({
      ...args,
      select: {
        codigoSerie: true,
        createdUserId: true,
        desctoUnidad: true,
        fechaRegistro: true,
        id: true,
        idFactura: true,

        items: {
          select: {
            id: true,
          },
        },

        precioUnidad: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: BillDetail })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "BillDetail",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async billDetail(
    @common.Param() params: BillDetailWhereUniqueInput
  ): Promise<BillDetail | null> {
    const result = await this.service.billDetail({
      where: params,
      select: {
        codigoSerie: true,
        createdUserId: true,
        desctoUnidad: true,
        fechaRegistro: true,
        id: true,
        idFactura: true,

        items: {
          select: {
            id: true,
          },
        },

        precioUnidad: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: BillDetail })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "BillDetail",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateBillDetail(
    @common.Param() params: BillDetailWhereUniqueInput,
    @common.Body() data: BillDetailUpdateInput
  ): Promise<BillDetail | null> {
    try {
      return await this.service.updateBillDetail({
        where: params,
        data: {
          ...data,

          items: {
            connect: data.items,
          },
        },
        select: {
          codigoSerie: true,
          createdUserId: true,
          desctoUnidad: true,
          fechaRegistro: true,
          id: true,
          idFactura: true,

          items: {
            select: {
              id: true,
            },
          },

          precioUnidad: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: BillDetail })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "BillDetail",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteBillDetail(
    @common.Param() params: BillDetailWhereUniqueInput
  ): Promise<BillDetail | null> {
    try {
      return await this.service.deleteBillDetail({
        where: params,
        select: {
          codigoSerie: true,
          createdUserId: true,
          desctoUnidad: true,
          fechaRegistro: true,
          id: true,
          idFactura: true,

          items: {
            select: {
              id: true,
            },
          },

          precioUnidad: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
