/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { FinancedAccessoryService } from "../financedAccessory.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { FinancedAccessoryCreateInput } from "./FinancedAccessoryCreateInput";
import { FinancedAccessory } from "./FinancedAccessory";
import { FinancedAccessoryFindManyArgs } from "./FinancedAccessoryFindManyArgs";
import { FinancedAccessoryWhereUniqueInput } from "./FinancedAccessoryWhereUniqueInput";
import { FinancedAccessoryUpdateInput } from "./FinancedAccessoryUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class FinancedAccessoryControllerBase {
  constructor(
    protected readonly service: FinancedAccessoryService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: FinancedAccessory })
  @nestAccessControl.UseRoles({
    resource: "FinancedAccessory",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createFinancedAccessory(
    @common.Body() data: FinancedAccessoryCreateInput
  ): Promise<FinancedAccessory> {
    return await this.service.createFinancedAccessory({
      data: {
        ...data,

        financedSales: {
          connect: data.financedSales,
        },

        items: {
          connect: data.items,
        },

        transactions: data.transactions
          ? {
              connect: data.transactions,
            }
          : undefined,
      },
      select: {
        cantUnidades: true,
        engancheAccesorio: true,

        financedSales: {
          select: {
            id: true,
          },
        },

        id: true,

        items: {
          select: {
            id: true,
          },
        },

        precioVenta: true,

        transactions: {
          select: {
            id: true,
          },
        },
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [FinancedAccessory] })
  @ApiNestedQuery(FinancedAccessoryFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "FinancedAccessory",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async financedAccessories(
    @common.Req() request: Request
  ): Promise<FinancedAccessory[]> {
    const args = plainToClass(FinancedAccessoryFindManyArgs, request.query);
    return this.service.financedAccessories({
      ...args,
      select: {
        cantUnidades: true,
        engancheAccesorio: true,

        financedSales: {
          select: {
            id: true,
          },
        },

        id: true,

        items: {
          select: {
            id: true,
          },
        },

        precioVenta: true,

        transactions: {
          select: {
            id: true,
          },
        },
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: FinancedAccessory })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "FinancedAccessory",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async financedAccessory(
    @common.Param() params: FinancedAccessoryWhereUniqueInput
  ): Promise<FinancedAccessory | null> {
    const result = await this.service.financedAccessory({
      where: params,
      select: {
        cantUnidades: true,
        engancheAccesorio: true,

        financedSales: {
          select: {
            id: true,
          },
        },

        id: true,

        items: {
          select: {
            id: true,
          },
        },

        precioVenta: true,

        transactions: {
          select: {
            id: true,
          },
        },
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: FinancedAccessory })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "FinancedAccessory",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateFinancedAccessory(
    @common.Param() params: FinancedAccessoryWhereUniqueInput,
    @common.Body() data: FinancedAccessoryUpdateInput
  ): Promise<FinancedAccessory | null> {
    try {
      return await this.service.updateFinancedAccessory({
        where: params,
        data: {
          ...data,

          financedSales: {
            connect: data.financedSales,
          },

          items: {
            connect: data.items,
          },

          transactions: data.transactions
            ? {
                connect: data.transactions,
              }
            : undefined,
        },
        select: {
          cantUnidades: true,
          engancheAccesorio: true,

          financedSales: {
            select: {
              id: true,
            },
          },

          id: true,

          items: {
            select: {
              id: true,
            },
          },

          precioVenta: true,

          transactions: {
            select: {
              id: true,
            },
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: FinancedAccessory })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "FinancedAccessory",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteFinancedAccessory(
    @common.Param() params: FinancedAccessoryWhereUniqueInput
  ): Promise<FinancedAccessory | null> {
    try {
      return await this.service.deleteFinancedAccessory({
        where: params,
        select: {
          cantUnidades: true,
          engancheAccesorio: true,

          financedSales: {
            select: {
              id: true,
            },
          },

          id: true,

          items: {
            select: {
              id: true,
            },
          },

          precioVenta: true,

          transactions: {
            select: {
              id: true,
            },
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
