/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { CashSaleDetailService } from "../cashSaleDetail.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { CashSaleDetailCreateInput } from "./CashSaleDetailCreateInput";
import { CashSaleDetail } from "./CashSaleDetail";
import { CashSaleDetailFindManyArgs } from "./CashSaleDetailFindManyArgs";
import { CashSaleDetailWhereUniqueInput } from "./CashSaleDetailWhereUniqueInput";
import { CashSaleDetailUpdateInput } from "./CashSaleDetailUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class CashSaleDetailControllerBase {
  constructor(
    protected readonly service: CashSaleDetailService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: CashSaleDetail })
  @nestAccessControl.UseRoles({
    resource: "CashSaleDetail",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createCashSaleDetail(
    @common.Body() data: CashSaleDetailCreateInput
  ): Promise<CashSaleDetail> {
    return await this.service.createCashSaleDetail({
      data: {
        ...data,

        items: {
          connect: data.items,
        },
      },
      select: {
        cantUnidades: true,
        cashSaleId: true,
        codigoBloqueo: true,
        codigoSerie: true,
        desctoAplicado: true,
        id: true,

        items: {
          select: {
            id: true,
          },
        },

        ivaUnidad: true,
        porcentajeDescto: true,
        precioUnidad: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [CashSaleDetail] })
  @ApiNestedQuery(CashSaleDetailFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "CashSaleDetail",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async cashSaleDetails(
    @common.Req() request: Request
  ): Promise<CashSaleDetail[]> {
    const args = plainToClass(CashSaleDetailFindManyArgs, request.query);
    return this.service.cashSaleDetails({
      ...args,
      select: {
        cantUnidades: true,
        cashSaleId: true,
        codigoBloqueo: true,
        codigoSerie: true,
        desctoAplicado: true,
        id: true,

        items: {
          select: {
            id: true,
          },
        },

        ivaUnidad: true,
        porcentajeDescto: true,
        precioUnidad: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: CashSaleDetail })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "CashSaleDetail",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async cashSaleDetail(
    @common.Param() params: CashSaleDetailWhereUniqueInput
  ): Promise<CashSaleDetail | null> {
    const result = await this.service.cashSaleDetail({
      where: params,
      select: {
        cantUnidades: true,
        cashSaleId: true,
        codigoBloqueo: true,
        codigoSerie: true,
        desctoAplicado: true,
        id: true,

        items: {
          select: {
            id: true,
          },
        },

        ivaUnidad: true,
        porcentajeDescto: true,
        precioUnidad: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: CashSaleDetail })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "CashSaleDetail",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateCashSaleDetail(
    @common.Param() params: CashSaleDetailWhereUniqueInput,
    @common.Body() data: CashSaleDetailUpdateInput
  ): Promise<CashSaleDetail | null> {
    try {
      return await this.service.updateCashSaleDetail({
        where: params,
        data: {
          ...data,

          items: {
            connect: data.items,
          },
        },
        select: {
          cantUnidades: true,
          cashSaleId: true,
          codigoBloqueo: true,
          codigoSerie: true,
          desctoAplicado: true,
          id: true,

          items: {
            select: {
              id: true,
            },
          },

          ivaUnidad: true,
          porcentajeDescto: true,
          precioUnidad: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: CashSaleDetail })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "CashSaleDetail",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteCashSaleDetail(
    @common.Param() params: CashSaleDetailWhereUniqueInput
  ): Promise<CashSaleDetail | null> {
    try {
      return await this.service.deleteCashSaleDetail({
        where: params,
        select: {
          cantUnidades: true,
          cashSaleId: true,
          codigoBloqueo: true,
          codigoSerie: true,
          desctoAplicado: true,
          id: true,

          items: {
            select: {
              id: true,
            },
          },

          ivaUnidad: true,
          porcentajeDescto: true,
          precioUnidad: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
