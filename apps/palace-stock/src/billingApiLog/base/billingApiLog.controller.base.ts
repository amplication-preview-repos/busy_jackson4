/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { BillingApiLogService } from "../billingApiLog.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { BillingApiLogCreateInput } from "./BillingApiLogCreateInput";
import { BillingApiLog } from "./BillingApiLog";
import { BillingApiLogFindManyArgs } from "./BillingApiLogFindManyArgs";
import { BillingApiLogWhereUniqueInput } from "./BillingApiLogWhereUniqueInput";
import { BillingApiLogUpdateInput } from "./BillingApiLogUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class BillingApiLogControllerBase {
  constructor(
    protected readonly service: BillingApiLogService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: BillingApiLog })
  @nestAccessControl.UseRoles({
    resource: "BillingApiLog",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createBillingApiLog(
    @common.Body() data: BillingApiLogCreateInput
  ): Promise<BillingApiLog> {
    return await this.service.createBillingApiLog({
      data: data,
      select: {
        billingId: true,
        callDate: true,
        calledMethod: true,
        callTime: true,
        calltrace: true,
        complementId: true,
        dataSend: true,
        headers: true,
        id: true,
        referenceId: true,
        response: true,
        responseDatetime: true,
        url: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [BillingApiLog] })
  @ApiNestedQuery(BillingApiLogFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "BillingApiLog",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async billingApiLogs(
    @common.Req() request: Request
  ): Promise<BillingApiLog[]> {
    const args = plainToClass(BillingApiLogFindManyArgs, request.query);
    return this.service.billingApiLogs({
      ...args,
      select: {
        billingId: true,
        callDate: true,
        calledMethod: true,
        callTime: true,
        calltrace: true,
        complementId: true,
        dataSend: true,
        headers: true,
        id: true,
        referenceId: true,
        response: true,
        responseDatetime: true,
        url: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: BillingApiLog })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "BillingApiLog",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async billingApiLog(
    @common.Param() params: BillingApiLogWhereUniqueInput
  ): Promise<BillingApiLog | null> {
    const result = await this.service.billingApiLog({
      where: params,
      select: {
        billingId: true,
        callDate: true,
        calledMethod: true,
        callTime: true,
        calltrace: true,
        complementId: true,
        dataSend: true,
        headers: true,
        id: true,
        referenceId: true,
        response: true,
        responseDatetime: true,
        url: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: BillingApiLog })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "BillingApiLog",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateBillingApiLog(
    @common.Param() params: BillingApiLogWhereUniqueInput,
    @common.Body() data: BillingApiLogUpdateInput
  ): Promise<BillingApiLog | null> {
    try {
      return await this.service.updateBillingApiLog({
        where: params,
        data: data,
        select: {
          billingId: true,
          callDate: true,
          calledMethod: true,
          callTime: true,
          calltrace: true,
          complementId: true,
          dataSend: true,
          headers: true,
          id: true,
          referenceId: true,
          response: true,
          responseDatetime: true,
          url: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: BillingApiLog })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "BillingApiLog",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteBillingApiLog(
    @common.Param() params: BillingApiLogWhereUniqueInput
  ): Promise<BillingApiLog | null> {
    try {
      return await this.service.deleteBillingApiLog({
        where: params,
        select: {
          billingId: true,
          callDate: true,
          calledMethod: true,
          callTime: true,
          calltrace: true,
          complementId: true,
          dataSend: true,
          headers: true,
          id: true,
          referenceId: true,
          response: true,
          responseDatetime: true,
          url: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
