/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { WarehouseGrantService } from "../warehouseGrant.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { WarehouseGrantCreateInput } from "./WarehouseGrantCreateInput";
import { WarehouseGrant } from "./WarehouseGrant";
import { WarehouseGrantFindManyArgs } from "./WarehouseGrantFindManyArgs";
import { WarehouseGrantWhereUniqueInput } from "./WarehouseGrantWhereUniqueInput";
import { WarehouseGrantUpdateInput } from "./WarehouseGrantUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class WarehouseGrantControllerBase {
  constructor(
    protected readonly service: WarehouseGrantService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: WarehouseGrant })
  @nestAccessControl.UseRoles({
    resource: "WarehouseGrant",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createWarehouseGrant(
    @common.Body() data: WarehouseGrantCreateInput
  ): Promise<WarehouseGrant> {
    return await this.service.createWarehouseGrant({
      data: {
        ...data,

        users: {
          connect: data.users,
        },

        warehouses: {
          connect: data.warehouses,
        },
      },
      select: {
        fechaAsignacion: true,
        id: true,
        mainWhouse: true,

        users: {
          select: {
            id: true,
          },
        },

        warehouses: {
          select: {
            id: true,
          },
        },
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [WarehouseGrant] })
  @ApiNestedQuery(WarehouseGrantFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "WarehouseGrant",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async warehouseGrants(
    @common.Req() request: Request
  ): Promise<WarehouseGrant[]> {
    const args = plainToClass(WarehouseGrantFindManyArgs, request.query);
    return this.service.warehouseGrants({
      ...args,
      select: {
        fechaAsignacion: true,
        id: true,
        mainWhouse: true,

        users: {
          select: {
            id: true,
          },
        },

        warehouses: {
          select: {
            id: true,
          },
        },
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: WarehouseGrant })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "WarehouseGrant",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async warehouseGrant(
    @common.Param() params: WarehouseGrantWhereUniqueInput
  ): Promise<WarehouseGrant | null> {
    const result = await this.service.warehouseGrant({
      where: params,
      select: {
        fechaAsignacion: true,
        id: true,
        mainWhouse: true,

        users: {
          select: {
            id: true,
          },
        },

        warehouses: {
          select: {
            id: true,
          },
        },
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: WarehouseGrant })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "WarehouseGrant",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateWarehouseGrant(
    @common.Param() params: WarehouseGrantWhereUniqueInput,
    @common.Body() data: WarehouseGrantUpdateInput
  ): Promise<WarehouseGrant | null> {
    try {
      return await this.service.updateWarehouseGrant({
        where: params,
        data: {
          ...data,

          users: {
            connect: data.users,
          },

          warehouses: {
            connect: data.warehouses,
          },
        },
        select: {
          fechaAsignacion: true,
          id: true,
          mainWhouse: true,

          users: {
            select: {
              id: true,
            },
          },

          warehouses: {
            select: {
              id: true,
            },
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: WarehouseGrant })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "WarehouseGrant",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteWarehouseGrant(
    @common.Param() params: WarehouseGrantWhereUniqueInput
  ): Promise<WarehouseGrant | null> {
    try {
      return await this.service.deleteWarehouseGrant({
        where: params,
        select: {
          fechaAsignacion: true,
          id: true,
          mainWhouse: true,

          users: {
            select: {
              id: true,
            },
          },

          warehouses: {
            select: {
              id: true,
            },
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
