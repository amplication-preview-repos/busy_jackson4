/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { KardexSeryService } from "../kardexSery.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { KardexSeryCreateInput } from "./KardexSeryCreateInput";
import { KardexSery } from "./KardexSery";
import { KardexSeryFindManyArgs } from "./KardexSeryFindManyArgs";
import { KardexSeryWhereUniqueInput } from "./KardexSeryWhereUniqueInput";
import { KardexSeryUpdateInput } from "./KardexSeryUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class KardexSeryControllerBase {
  constructor(
    protected readonly service: KardexSeryService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: KardexSery })
  @nestAccessControl.UseRoles({
    resource: "KardexSery",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createKardexSery(
    @common.Body() data: KardexSeryCreateInput
  ): Promise<KardexSery> {
    return await this.service.createKardexSery({
      data: {
        ...data,

        items: {
          connect: data.items,
        },

        transactions: {
          connect: data.transactions,
        },

        warehouses: {
          connect: data.warehouses,
        },
      },
      select: {
        codigoBloqueo: true,
        id: true,

        items: {
          select: {
            id: true,
          },
        },

        otraSerie: true,
        serialCode: true,

        transactions: {
          select: {
            id: true,
          },
        },

        warehouses: {
          select: {
            id: true,
          },
        },
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [KardexSery] })
  @ApiNestedQuery(KardexSeryFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "KardexSery",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async kardexSeries(@common.Req() request: Request): Promise<KardexSery[]> {
    const args = plainToClass(KardexSeryFindManyArgs, request.query);
    return this.service.kardexSeries({
      ...args,
      select: {
        codigoBloqueo: true,
        id: true,

        items: {
          select: {
            id: true,
          },
        },

        otraSerie: true,
        serialCode: true,

        transactions: {
          select: {
            id: true,
          },
        },

        warehouses: {
          select: {
            id: true,
          },
        },
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: KardexSery })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "KardexSery",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async kardexSery(
    @common.Param() params: KardexSeryWhereUniqueInput
  ): Promise<KardexSery | null> {
    const result = await this.service.kardexSery({
      where: params,
      select: {
        codigoBloqueo: true,
        id: true,

        items: {
          select: {
            id: true,
          },
        },

        otraSerie: true,
        serialCode: true,

        transactions: {
          select: {
            id: true,
          },
        },

        warehouses: {
          select: {
            id: true,
          },
        },
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: KardexSery })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "KardexSery",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateKardexSery(
    @common.Param() params: KardexSeryWhereUniqueInput,
    @common.Body() data: KardexSeryUpdateInput
  ): Promise<KardexSery | null> {
    try {
      return await this.service.updateKardexSery({
        where: params,
        data: {
          ...data,

          items: {
            connect: data.items,
          },

          transactions: {
            connect: data.transactions,
          },

          warehouses: {
            connect: data.warehouses,
          },
        },
        select: {
          codigoBloqueo: true,
          id: true,

          items: {
            select: {
              id: true,
            },
          },

          otraSerie: true,
          serialCode: true,

          transactions: {
            select: {
              id: true,
            },
          },

          warehouses: {
            select: {
              id: true,
            },
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: KardexSery })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "KardexSery",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteKardexSery(
    @common.Param() params: KardexSeryWhereUniqueInput
  ): Promise<KardexSery | null> {
    try {
      return await this.service.deleteKardexSery({
        where: params,
        select: {
          codigoBloqueo: true,
          id: true,

          items: {
            select: {
              id: true,
            },
          },

          otraSerie: true,
          serialCode: true,

          transactions: {
            select: {
              id: true,
            },
          },

          warehouses: {
            select: {
              id: true,
            },
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
