/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { FinancedSalesLogService } from "../financedSalesLog.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { FinancedSalesLogCreateInput } from "./FinancedSalesLogCreateInput";
import { FinancedSalesLog } from "./FinancedSalesLog";
import { FinancedSalesLogFindManyArgs } from "./FinancedSalesLogFindManyArgs";
import { FinancedSalesLogWhereUniqueInput } from "./FinancedSalesLogWhereUniqueInput";
import { FinancedSalesLogUpdateInput } from "./FinancedSalesLogUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class FinancedSalesLogControllerBase {
  constructor(
    protected readonly service: FinancedSalesLogService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: FinancedSalesLog })
  @nestAccessControl.UseRoles({
    resource: "FinancedSalesLog",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createFinancedSalesLog(
    @common.Body() data: FinancedSalesLogCreateInput
  ): Promise<FinancedSalesLog> {
    return await this.service.createFinancedSalesLog({
      data: {
        ...data,

        financedSales: {
          connect: data.financedSales,
        },
      },
      select: {
        fechaUltimaEdicion: true,

        financedSales: {
          select: {
            id: true,
          },
        },

        id: true,
        idUsrUltimaEdicion: true,
        infoFinanc: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [FinancedSalesLog] })
  @ApiNestedQuery(FinancedSalesLogFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "FinancedSalesLog",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async financedSalesLogs(
    @common.Req() request: Request
  ): Promise<FinancedSalesLog[]> {
    const args = plainToClass(FinancedSalesLogFindManyArgs, request.query);
    return this.service.financedSalesLogs({
      ...args,
      select: {
        fechaUltimaEdicion: true,

        financedSales: {
          select: {
            id: true,
          },
        },

        id: true,
        idUsrUltimaEdicion: true,
        infoFinanc: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: FinancedSalesLog })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "FinancedSalesLog",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async financedSalesLog(
    @common.Param() params: FinancedSalesLogWhereUniqueInput
  ): Promise<FinancedSalesLog | null> {
    const result = await this.service.financedSalesLog({
      where: params,
      select: {
        fechaUltimaEdicion: true,

        financedSales: {
          select: {
            id: true,
          },
        },

        id: true,
        idUsrUltimaEdicion: true,
        infoFinanc: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: FinancedSalesLog })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "FinancedSalesLog",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateFinancedSalesLog(
    @common.Param() params: FinancedSalesLogWhereUniqueInput,
    @common.Body() data: FinancedSalesLogUpdateInput
  ): Promise<FinancedSalesLog | null> {
    try {
      return await this.service.updateFinancedSalesLog({
        where: params,
        data: {
          ...data,

          financedSales: {
            connect: data.financedSales,
          },
        },
        select: {
          fechaUltimaEdicion: true,

          financedSales: {
            select: {
              id: true,
            },
          },

          id: true,
          idUsrUltimaEdicion: true,
          infoFinanc: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: FinancedSalesLog })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "FinancedSalesLog",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteFinancedSalesLog(
    @common.Param() params: FinancedSalesLogWhereUniqueInput
  ): Promise<FinancedSalesLog | null> {
    try {
      return await this.service.deleteFinancedSalesLog({
        where: params,
        select: {
          fechaUltimaEdicion: true,

          financedSales: {
            select: {
              id: true,
            },
          },

          id: true,
          idUsrUltimaEdicion: true,
          infoFinanc: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
