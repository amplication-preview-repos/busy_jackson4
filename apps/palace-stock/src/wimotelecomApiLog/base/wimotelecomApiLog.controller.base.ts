/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { WimotelecomApiLogService } from "../wimotelecomApiLog.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { WimotelecomApiLogCreateInput } from "./WimotelecomApiLogCreateInput";
import { WimotelecomApiLog } from "./WimotelecomApiLog";
import { WimotelecomApiLogFindManyArgs } from "./WimotelecomApiLogFindManyArgs";
import { WimotelecomApiLogWhereUniqueInput } from "./WimotelecomApiLogWhereUniqueInput";
import { WimotelecomApiLogUpdateInput } from "./WimotelecomApiLogUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class WimotelecomApiLogControllerBase {
  constructor(
    protected readonly service: WimotelecomApiLogService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: WimotelecomApiLog })
  @nestAccessControl.UseRoles({
    resource: "WimotelecomApiLog",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createWimotelecomApiLog(
    @common.Body() data: WimotelecomApiLogCreateInput
  ): Promise<WimotelecomApiLog> {
    return await this.service.createWimotelecomApiLog({
      data: data,
      select: {
        callDate: true,
        calledMethod: true,
        callTime: true,
        calltrace: true,
        dataSend: true,
        headers: true,
        id: true,
        response: true,
        responseDatetime: true,
        url: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [WimotelecomApiLog] })
  @ApiNestedQuery(WimotelecomApiLogFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "WimotelecomApiLog",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async wimotelecomApiLogs(
    @common.Req() request: Request
  ): Promise<WimotelecomApiLog[]> {
    const args = plainToClass(WimotelecomApiLogFindManyArgs, request.query);
    return this.service.wimotelecomApiLogs({
      ...args,
      select: {
        callDate: true,
        calledMethod: true,
        callTime: true,
        calltrace: true,
        dataSend: true,
        headers: true,
        id: true,
        response: true,
        responseDatetime: true,
        url: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: WimotelecomApiLog })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "WimotelecomApiLog",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async wimotelecomApiLog(
    @common.Param() params: WimotelecomApiLogWhereUniqueInput
  ): Promise<WimotelecomApiLog | null> {
    const result = await this.service.wimotelecomApiLog({
      where: params,
      select: {
        callDate: true,
        calledMethod: true,
        callTime: true,
        calltrace: true,
        dataSend: true,
        headers: true,
        id: true,
        response: true,
        responseDatetime: true,
        url: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: WimotelecomApiLog })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "WimotelecomApiLog",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateWimotelecomApiLog(
    @common.Param() params: WimotelecomApiLogWhereUniqueInput,
    @common.Body() data: WimotelecomApiLogUpdateInput
  ): Promise<WimotelecomApiLog | null> {
    try {
      return await this.service.updateWimotelecomApiLog({
        where: params,
        data: data,
        select: {
          callDate: true,
          calledMethod: true,
          callTime: true,
          calltrace: true,
          dataSend: true,
          headers: true,
          id: true,
          response: true,
          responseDatetime: true,
          url: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: WimotelecomApiLog })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "WimotelecomApiLog",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteWimotelecomApiLog(
    @common.Param() params: WimotelecomApiLogWhereUniqueInput
  ): Promise<WimotelecomApiLog | null> {
    try {
      return await this.service.deleteWimotelecomApiLog({
        where: params,
        select: {
          callDate: true,
          calledMethod: true,
          callTime: true,
          calltrace: true,
          dataSend: true,
          headers: true,
          id: true,
          response: true,
          responseDatetime: true,
          url: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
