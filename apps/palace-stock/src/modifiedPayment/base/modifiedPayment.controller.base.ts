/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { ModifiedPaymentService } from "../modifiedPayment.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { ModifiedPaymentCreateInput } from "./ModifiedPaymentCreateInput";
import { ModifiedPayment } from "./ModifiedPayment";
import { ModifiedPaymentFindManyArgs } from "./ModifiedPaymentFindManyArgs";
import { ModifiedPaymentWhereUniqueInput } from "./ModifiedPaymentWhereUniqueInput";
import { ModifiedPaymentUpdateInput } from "./ModifiedPaymentUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class ModifiedPaymentControllerBase {
  constructor(
    protected readonly service: ModifiedPaymentService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: ModifiedPayment })
  @nestAccessControl.UseRoles({
    resource: "ModifiedPayment",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createModifiedPayment(
    @common.Body() data: ModifiedPaymentCreateInput
  ): Promise<ModifiedPayment> {
    return await this.service.createModifiedPayment({
      data: {
        ...data,

        financedSales: data.financedSales
          ? {
              connect: data.financedSales,
            }
          : undefined,

        payments: data.payments
          ? {
              connect: data.payments,
            }
          : undefined,
      },
      select: {
        comentarios: true,
        estatus_pago: true,
        fechaHoraRegistro: true,
        fechaUltimaEdicion: true,

        financedSales: {
          select: {
            id: true,
          },
        },

        id: true,
        idUsrUltimaEdicion: true,
        infoPago: true,
        numPago: true,

        payments: {
          select: {
            id: true,
          },
        },
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [ModifiedPayment] })
  @ApiNestedQuery(ModifiedPaymentFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "ModifiedPayment",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async modifiedPayments(
    @common.Req() request: Request
  ): Promise<ModifiedPayment[]> {
    const args = plainToClass(ModifiedPaymentFindManyArgs, request.query);
    return this.service.modifiedPayments({
      ...args,
      select: {
        comentarios: true,
        estatus_pago: true,
        fechaHoraRegistro: true,
        fechaUltimaEdicion: true,

        financedSales: {
          select: {
            id: true,
          },
        },

        id: true,
        idUsrUltimaEdicion: true,
        infoPago: true,
        numPago: true,

        payments: {
          select: {
            id: true,
          },
        },
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: ModifiedPayment })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "ModifiedPayment",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async modifiedPayment(
    @common.Param() params: ModifiedPaymentWhereUniqueInput
  ): Promise<ModifiedPayment | null> {
    const result = await this.service.modifiedPayment({
      where: params,
      select: {
        comentarios: true,
        estatus_pago: true,
        fechaHoraRegistro: true,
        fechaUltimaEdicion: true,

        financedSales: {
          select: {
            id: true,
          },
        },

        id: true,
        idUsrUltimaEdicion: true,
        infoPago: true,
        numPago: true,

        payments: {
          select: {
            id: true,
          },
        },
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: ModifiedPayment })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "ModifiedPayment",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateModifiedPayment(
    @common.Param() params: ModifiedPaymentWhereUniqueInput,
    @common.Body() data: ModifiedPaymentUpdateInput
  ): Promise<ModifiedPayment | null> {
    try {
      return await this.service.updateModifiedPayment({
        where: params,
        data: {
          ...data,

          financedSales: data.financedSales
            ? {
                connect: data.financedSales,
              }
            : undefined,

          payments: data.payments
            ? {
                connect: data.payments,
              }
            : undefined,
        },
        select: {
          comentarios: true,
          estatus_pago: true,
          fechaHoraRegistro: true,
          fechaUltimaEdicion: true,

          financedSales: {
            select: {
              id: true,
            },
          },

          id: true,
          idUsrUltimaEdicion: true,
          infoPago: true,
          numPago: true,

          payments: {
            select: {
              id: true,
            },
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: ModifiedPayment })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "ModifiedPayment",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteModifiedPayment(
    @common.Param() params: ModifiedPaymentWhereUniqueInput
  ): Promise<ModifiedPayment | null> {
    try {
      return await this.service.deleteModifiedPayment({
        where: params,
        select: {
          comentarios: true,
          estatus_pago: true,
          fechaHoraRegistro: true,
          fechaUltimaEdicion: true,

          financedSales: {
            select: {
              id: true,
            },
          },

          id: true,
          idUsrUltimaEdicion: true,
          infoPago: true,
          numPago: true,

          payments: {
            select: {
              id: true,
            },
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
