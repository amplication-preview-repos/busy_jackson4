/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { BankReceiptService } from "../bankReceipt.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { BankReceiptCreateInput } from "./BankReceiptCreateInput";
import { BankReceipt } from "./BankReceipt";
import { BankReceiptFindManyArgs } from "./BankReceiptFindManyArgs";
import { BankReceiptWhereUniqueInput } from "./BankReceiptWhereUniqueInput";
import { BankReceiptUpdateInput } from "./BankReceiptUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class BankReceiptControllerBase {
  constructor(
    protected readonly service: BankReceiptService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: BankReceipt })
  @nestAccessControl.UseRoles({
    resource: "BankReceipt",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createBankReceipt(
    @common.Body() data: BankReceiptCreateInput
  ): Promise<BankReceipt> {
    return await this.service.createBankReceipt({
      data: {
        ...data,

        cashCounts: {
          connect: data.cashCounts,
        },
      },
      select: {
        autorizacion: true,
        banco: true,

        cashCounts: {
          select: {
            id: true,
          },
        },

        createdUserId: true,
        estatus_ficha: true,
        estatus_validacion: true,
        fechaDeposito: true,
        fechaRegistro: true,
        fechaValidacion: true,
        folio: true,
        id: true,
        idBanco: true,
        montoDeposito: true,
        tipo_deposito: true,
        validUserId: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [BankReceipt] })
  @ApiNestedQuery(BankReceiptFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "BankReceipt",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async bankReceipts(@common.Req() request: Request): Promise<BankReceipt[]> {
    const args = plainToClass(BankReceiptFindManyArgs, request.query);
    return this.service.bankReceipts({
      ...args,
      select: {
        autorizacion: true,
        banco: true,

        cashCounts: {
          select: {
            id: true,
          },
        },

        createdUserId: true,
        estatus_ficha: true,
        estatus_validacion: true,
        fechaDeposito: true,
        fechaRegistro: true,
        fechaValidacion: true,
        folio: true,
        id: true,
        idBanco: true,
        montoDeposito: true,
        tipo_deposito: true,
        validUserId: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: BankReceipt })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "BankReceipt",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async bankReceipt(
    @common.Param() params: BankReceiptWhereUniqueInput
  ): Promise<BankReceipt | null> {
    const result = await this.service.bankReceipt({
      where: params,
      select: {
        autorizacion: true,
        banco: true,

        cashCounts: {
          select: {
            id: true,
          },
        },

        createdUserId: true,
        estatus_ficha: true,
        estatus_validacion: true,
        fechaDeposito: true,
        fechaRegistro: true,
        fechaValidacion: true,
        folio: true,
        id: true,
        idBanco: true,
        montoDeposito: true,
        tipo_deposito: true,
        validUserId: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: BankReceipt })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "BankReceipt",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateBankReceipt(
    @common.Param() params: BankReceiptWhereUniqueInput,
    @common.Body() data: BankReceiptUpdateInput
  ): Promise<BankReceipt | null> {
    try {
      return await this.service.updateBankReceipt({
        where: params,
        data: {
          ...data,

          cashCounts: {
            connect: data.cashCounts,
          },
        },
        select: {
          autorizacion: true,
          banco: true,

          cashCounts: {
            select: {
              id: true,
            },
          },

          createdUserId: true,
          estatus_ficha: true,
          estatus_validacion: true,
          fechaDeposito: true,
          fechaRegistro: true,
          fechaValidacion: true,
          folio: true,
          id: true,
          idBanco: true,
          montoDeposito: true,
          tipo_deposito: true,
          validUserId: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: BankReceipt })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "BankReceipt",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteBankReceipt(
    @common.Param() params: BankReceiptWhereUniqueInput
  ): Promise<BankReceipt | null> {
    try {
      return await this.service.deleteBankReceipt({
        where: params,
        select: {
          autorizacion: true,
          banco: true,

          cashCounts: {
            select: {
              id: true,
            },
          },

          createdUserId: true,
          estatus_ficha: true,
          estatus_validacion: true,
          fechaDeposito: true,
          fechaRegistro: true,
          fechaValidacion: true,
          folio: true,
          id: true,
          idBanco: true,
          montoDeposito: true,
          tipo_deposito: true,
          validUserId: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
