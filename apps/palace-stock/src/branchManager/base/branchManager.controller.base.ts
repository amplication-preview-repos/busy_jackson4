/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { BranchManagerService } from "../branchManager.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { BranchManagerCreateInput } from "./BranchManagerCreateInput";
import { BranchManager } from "./BranchManager";
import { BranchManagerFindManyArgs } from "./BranchManagerFindManyArgs";
import { BranchManagerWhereUniqueInput } from "./BranchManagerWhereUniqueInput";
import { BranchManagerUpdateInput } from "./BranchManagerUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class BranchManagerControllerBase {
  constructor(
    protected readonly service: BranchManagerService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: BranchManager })
  @nestAccessControl.UseRoles({
    resource: "BranchManager",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createBranchManager(
    @common.Body() data: BranchManagerCreateInput
  ): Promise<BranchManager> {
    return await this.service.createBranchManager({
      data: {
        ...data,

        collaborators: data.collaborators
          ? {
              connect: data.collaborators,
            }
          : undefined,

        users: {
          connect: data.users,
        },

        warehouses: data.warehouses
          ? {
              connect: data.warehouses,
            }
          : undefined,
      },
      select: {
        cargo: true,

        collaborators: {
          select: {
            id: true,
          },
        },

        fechaIniciaCargo: true,
        id: true,
        numSucursal: true,

        users: {
          select: {
            id: true,
          },
        },

        warehouses: {
          select: {
            id: true,
          },
        },
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [BranchManager] })
  @ApiNestedQuery(BranchManagerFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "BranchManager",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async branchManagers(
    @common.Req() request: Request
  ): Promise<BranchManager[]> {
    const args = plainToClass(BranchManagerFindManyArgs, request.query);
    return this.service.branchManagers({
      ...args,
      select: {
        cargo: true,

        collaborators: {
          select: {
            id: true,
          },
        },

        fechaIniciaCargo: true,
        id: true,
        numSucursal: true,

        users: {
          select: {
            id: true,
          },
        },

        warehouses: {
          select: {
            id: true,
          },
        },
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: BranchManager })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "BranchManager",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async branchManager(
    @common.Param() params: BranchManagerWhereUniqueInput
  ): Promise<BranchManager | null> {
    const result = await this.service.branchManager({
      where: params,
      select: {
        cargo: true,

        collaborators: {
          select: {
            id: true,
          },
        },

        fechaIniciaCargo: true,
        id: true,
        numSucursal: true,

        users: {
          select: {
            id: true,
          },
        },

        warehouses: {
          select: {
            id: true,
          },
        },
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: BranchManager })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "BranchManager",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateBranchManager(
    @common.Param() params: BranchManagerWhereUniqueInput,
    @common.Body() data: BranchManagerUpdateInput
  ): Promise<BranchManager | null> {
    try {
      return await this.service.updateBranchManager({
        where: params,
        data: {
          ...data,

          collaborators: data.collaborators
            ? {
                connect: data.collaborators,
              }
            : undefined,

          users: {
            connect: data.users,
          },

          warehouses: data.warehouses
            ? {
                connect: data.warehouses,
              }
            : undefined,
        },
        select: {
          cargo: true,

          collaborators: {
            select: {
              id: true,
            },
          },

          fechaIniciaCargo: true,
          id: true,
          numSucursal: true,

          users: {
            select: {
              id: true,
            },
          },

          warehouses: {
            select: {
              id: true,
            },
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: BranchManager })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "BranchManager",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteBranchManager(
    @common.Param() params: BranchManagerWhereUniqueInput
  ): Promise<BranchManager | null> {
    try {
      return await this.service.deleteBranchManager({
        where: params,
        select: {
          cargo: true,

          collaborators: {
            select: {
              id: true,
            },
          },

          fechaIniciaCargo: true,
          id: true,
          numSucursal: true,

          users: {
            select: {
              id: true,
            },
          },

          warehouses: {
            select: {
              id: true,
            },
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
